# A5 — Phase 3 Audit Fragment (doc 06 + 09)

> **范围**：A5 新派分析师，无 Phase 1 pi 源码 anchor；审 doc-06 / doc-09（共 383 行）。
> **总裁决（A5 范围内）**：doc-06 §6.1-§6.5 描述 pi 现状有教学价值，§6.7 \"keep regardless of backend\" 4 条中 ≥2 条直接违反 phase2；doc-09 主体结构（core+harness 双层 / session 独立包 / 产品层混入）与 phase2 互不相容，整体不可作为 tau 核心模块布局。
> **重要 evidence 升级**：A5 找到 doc-06 行 86（`pi.on session:compact` 一等事件）+ 行 92（`pi.registerProvider runtime`）= 与 A1 `custom-compaction.ts` 三方互证 G4 stated 理由 #2 严重证伪 + R4 违反是 pi 公共扩展架构默认。
> **本文件**：A5 报告原文 + Q-First 3 答 + Phase 3 整合稿引用的 fragment 升级版（conversation-only 转 fragment）。

---

## §A — doc-06 self-extensibility (181 行)

> **审审框架关键区分**：§6.1–§6.5 是描述（讲 pi 现状），§6.6 + §6.7 是规范（给 tau Go port 建议）。规范段直接受 phase2 标尺约束；描述段的违反等价于"pi 这样做暴露的反模式"，对 tau 是反面教材。

### ✅ 保留项 (4)

- **[§6.1 行 32]** `(api) => void` 默认函数注册风格 — 与 phase2 §2.2 SessionOptions + §5.1 closure 注册一致
- **[§6.7 行 175 第 1 条]** \"Extension shape: (api) => void. Simple registration model.\" — 与 phase2 一致 ✅
- **[§6.6 行 154-155 Option D]** \"Go `plugin` package — AVOID. UNIX-only, version-fragile, can't be reloaded, can't be sandboxed.\" — 匹配 R4 / C1（plugin 一旦载入污染整个进程地址空间，无法 session-scoped）
- **[§6.6 行 121 整段 Option A \"Embedded scripting (RECOMMENDED)\"]** — Starlark / goja / Lua 推荐 + host-object-exposing 嵌入 runtime。匹配 phase2 §1 \"Tau is not\"

### ⚠️ 冲突项 (4)

- **⚠️-06.1 [§6.7 行 178 第 3 条] Pre-bind queueing 模式 vs phase2 NewSession 唯一构造**
  docs：\"extensions queue `pendingProviderRegistrations` during load and flush on `bindCore()`...\"
  phase2 §2.2：NewSession 是 Session 唯一构造；构造完成后才能 Register。无 pre-bind 阶段。
  **裁决：docs 错（pi-specific 解决 pi-specific 问题）**——pi pre-bind queue 是解决 \"extension 文件先于 core 实例载入\" 生命周期错位的补丁；phase2 把 Session 立成一等抽象后此问题消失。L1 命中 C1 + R4。

- **⚠️-06.2 [§6.7 行 179 第 4 条] Two-tier API: registration vs action methods on `ctx`**
  docs：保留 pi 的 ctx 双层 API。`ctx` 含 `setSessionName / setLabel / setActiveTools / setModel / setThinkingLevel / fork / newSession / switchSession / reload / exec`（§6.3 行 99-104）。
  phase2 §2.2：mutable 状态都在 Session 字段；`Session.Tools.SetActive([]string)` 这类 setter 直接挂 Session 对应 sub-component。无"action ctx"概念。Go `context.Context` 与 pi \"ctx\" 不是一个东西。
  **裁决：docs 错（pi 命名 + pi 概念混淆）**——pi ctx 把"会话状态访问"和"产品层动作"糅在一个对象 → 违反 R5 + C7。

- **⚠️-06.3 [§6.3 行 89] `pi.on(event, handler)` 单 API 暗分语义（C5 字面违反 + G4 evidence 升级版）**
  docs：handler 选择性返回 patch mutate flow（"chain-of-responsibility, same as harness hook bus"）——用同一 API 同时做 \"subscribe (read-only)\" 和 \"transform (chain mutate)\"，靠 handler 是否返回值暗分语义。
  phase2 §2.8（C5 落地）：Chain[T] 与 LastWins[T] / Observe 必须命名 + 类型签名都不同；编译期阻止 observer/transformer 误用。
  **裁决：docs 错（C5 + P1 review-pain<user-pain）**。
  **G4 evidence 三方互证升级**：行 86 `session lifecycle (start/end/compact/tree/fork/switch)` 把 `session:compact` 作为 extension API 一等事件 = A1 `examples/extensions/custom-compaction.ts:9-115` + agent-session.ts:1665/1938 hook 落点的**公共扩展 API 层暴露**。pi.on 单 API 暗分语义模式 + compact 一等事件 = 生产环境真实使用的 P1 反例（用户痛已发生过）。

- **⚠️-06.4 [§6.3 行 92 + §6.1 行 32] `pi.registerProvider` runtime 注册（C1+R4 + 架构级 evidence 升级版）**
  docs：行 92 \"register a custom AI provider at runtime (queued during load, flushed on `bindCore()`)\"；行 32 \"extension registers... providers\"。
  phase2 §2.4：ProviderRegistry 是 Session 字段，Session-scoped 同步 Register/Get，无 runtime 全局注册。
  **裁决：docs 错（C1 + R4 + Phase 1 D4 反例延续）**。
  **架构级 evidence chain**：行 92 (registerProvider runtime) + 行 32 (extension 注册 providers) + A1 custom-compaction.ts → pi 公共扩展架构标准模式 = "extension 注册 cheap-model provider + 订阅 session:compact + 替换 compaction 实现"。**runtime 注册不是设计意外，是 pi 公开支持的 first-class 扩展路径**——R4 违反不是孤例 bug 而是架构性默认。

### ➕ 补充项 (3)

- **[§6.5 行 113-122] invalidate-and-throw stale-ctx 模式** — phase2 v1.0 没有 fork/newSession/switchSession 入口（推 Phase 5+）。一旦未来加这些入口，闭包/handle 失效问题立刻浮出。**Owner 已裁决归 ➕ 补充清单 + Phase 5+ trigger，不升级为战略**。phase2 当前不引此机制，但 Phase 5+ 引入会话替换入口时**必须**配套此模式。
- **[§6.4 行 107-110] Sandboxing: NONE — 用户脚本 vs untrusted plugin marketplace** — phase2 §1 extension 在产品层，sandbox 是产品层职责。**否（产品层）**——但 phase2 §5 或 §1 应补一句"未来产品层若做 untrusted plugin 必须自带 sandbox 决策；tau 核心不强制也不阻止"避免 ambiguity。
- **[§6.6 Option C 行 148] Wasm via wazero** — 与 ➕-2 配套：若产品层走 untrusted 路线，wazero 是当前 Go 生态最 phase2-friendly 的方案（纯 Go 无 cgo / deterministic / 可与 R4 Session-scoped 嵌合）。**否（产品层选型 hint，不入主稿）**。

### ❌ 弃用项 (3)

- **❌-06.1 [§6.3 行 96-97] `registerEditor / registerWidget / registerKeybinding / registerMessageRenderer / registerAutocompleteProvider`** — 5 个 TUI / 渲染相关 register 入口。**R5 违反 + C7 违反 + 类名延续**。phase2 §1 \"core 包内禁具体 provider 名和 tool 名\"——TUI / editor / widget / keybinding 是 pi-tui 包概念。**弃用**：tau 若 Phase 5+ 做 extension 仅暴露 `RegisterTool / RegisterCommand / RegisterProvider` 三类与核心抽象对齐的入口。

- **❌-06.2 [§6.3 行 89-90] \"90+ event types\"** — \"agent lifecycle, message lifecycle, turn lifecycle, tool execution, session lifecycle (start/end/compact/tree/fork/switch), provider request/response, input events, resources discovery, model/thinking-level changes\"。**R2 违反**——phase2 §2.5 sealed interface + 9 类具体事件；扩到 90+ = 严重过抽象。列表内含 \"compact\"（G4 已撤回）、\"tree/fork/switch\"（推 Phase 5+）、\"resources discovery\"（产品层）、\"thinking-level\"（产品层）、\"input events\"（产品层 TUI）。**A2 §A 11 接入点已映射验证**：90+ 中 ≥80% 是产品层耦合事件。**弃用 90+ 表面**；保留 §2.5 的 9 类核心事件 + 产品层自由扩 product-specific event。

- **❌-06.3 [§6.3 行 99-104] `ctx.setModel / setThinkingLevel / setLabel / fork / newSession / switchSession / reload / exec` 作为 ExtensionAPI 一等行动入口** — `exec`（执行 shell）是 coding-agent；`setThinkingLevel` 是 OpenAI/Anthropic-specific（违反 R5）；`fork / switchSession` 是会话树（产品层）；`reload` 是 pi-extension specific。**弃用整个 ctx 行动列表**；tau 若有 extension 仅暴露 Session 字段对应的 setter，不引入新 ctx 抽象。

---

## §B — doc-09 go-module-structure (202 行)

### ✅ 保留项 (5)

- **[§9.1 行 35-46] `providers/` ONE FILE PER WIRE PROTOCOL** — A2 已报 wire-protocol 4 evidence 全 ✅；与 phase2 §2.4 函数式 Provider + Session-scoped Registry 兼容。**docs/09 最锋利的正确判断**之一。
- **[§9.1 行 38-40] `providers/faux/` 与正式 provider 同模块；prompt-cache 模拟** — 与 phase2 §5 minimum demo case + handoff B2 必抄一致
- **[§9.1 行 41] `providers/event_stream.go # typed channel-based EventStream`** — 与 phase2 §2.4 channel 决策（W3-lite 差异 A 有意识取舍）一致
- **[§9.2 行 134-138 step 2 + step 3] \"core/ using only the faux provider...90% of the agent's behavior is testable\"** — 与 phase2 §5 demo case 哲学一致
- **[§9.4 行 196 v0.1 milestone] 明文写 \"no compaction yet, no hook bus yet\"** — 与 phase2 §6 G4（撤回前的初始砍）方向一致——v0.1 不引 compaction

### ⚠️ 冲突项 (7)（含 ⚠️-1 根因 + 4 派生 + 2 独立 + 3 重标 scope 错位）

#### ⚠️-09.1 — 根因 (其他 ⚠️ 都派生于此)
**[§9.1 行 5-18] `core/` + `harness/` 双层包结构** — `harness/harness.go` 注释"AgentHarness: phase machine, prompt/steer/followUp/compact"。
phase2 §1 明文砍 AgentHarness 只留 Loop。
**裁决：docs 错（L2 phase2 §6 已决策 + L1 R2/C4）**——doc 09 双层包是 D1 #1 反复反模式（Agent vs AgentHarness）的 Go 形态再实例化。`harness/harness.go` 文件头列\"prompt/steer/followUp/compact\"四入口 = phase2 已决策三概念（D1 双层 / steer-followUp / compact）的复活。**tau 等价合法包是 `core/` 一层（含 Loop / Session / Transcript / 3 Registry / Hook / AgentEvent），不需要 harness/**。

#### ⚠️-09.2 派生 — `session/` 独立顶层包 + JSONL/fork/tree
**[§9.1 行 27-31]** Session 被降级为外部包 + 持久化与 fork tree 植入核心模块。
phase2 §2.2：Session 是 `package core` 一等抽象；phase2 完全没提持久化（推产品层）。
**裁决：docs 错（双重错位：scope 错位 + 产品层混入）**。R5 + C7 违反；`MemorySessionStorage (tests)` 暗示 SessionStorage interface = phase2 §2.3 Transcript 接口的重复抽象（违反 R2）。

#### ⚠️-09.3 派生 — `core/queue.go # PendingMessageQueue`
**[§9.1 行 9]** core 包内含 Pending Message Queue。
phase2 §2.1 + §6：steer/followUp 推 Phase 5+，pi 4 入口 → tau 2 入口。Pending queue 是 steer/followUp 支持基础设施。
**裁决：docs 错（已砍概念延续）**——为已被推迟的功能预留 mutable state，违反 yagni + R4。

#### ⚠️-09.4 降级 pending-Phase 4 重审 — `harness/compaction/` 子树
**[§9.1 行 22-27 + §9.2 step 5 行 149-159]** 5 个文件 + step 5 列 7 条 compaction 测试（含 `session_before_compact` hook）。
phase2 §6 G4：原裁决"砍 compaction execute hook"。
**裁决变更（Phase 3）**：从"L2 G4 已决策不重开"**降级为 ⚠️-pending-Phase 4 重审**。
理由：G4 stated 理由 #2 \"已知用户 0\" 被 A1+A5 三方 evidence 互证证伪（doc-06 行 86 + 行 92 + custom-compaction.ts 公共扩展 API + 官方 example 两层覆盖）。anchor 保留作 Phase 4 G4 重审 evidence；实质决策悬空。**重审真问题**（phase2 §B.7 self-doubt #7 trigger）：不是 hook 该不该有，而是 phase2 §2.3 Transcript 是否暴露 \"summarize → replace tail\" 写入入口。

#### ⚠️-09.5 派生 — `harness/system_prompt.go # dynamic system prompt + skills XML block`
**[§9.1 行 16]** system_prompt 子模块绑定 \"skills XML block\" 格式。
phase2 §2.2：SystemPromptFn 是 closure；核心层不知 SKILL.md / skills 概念（§2.9 G3）。
**裁决：docs 错（R5 + G3 双违反）**——把"skills XML block"烤进 harness/ 文件名 = 核心层确认 skills 概念存在 + 确认特定 XML 编码格式。

#### ⚠️-09.6 派生 — `harness/messages.go # AgentMessage <-> LLM Message conversion`
**[§9.1 行 17]** harness 层做 AgentMessage 到 Provider Message 的转换。
phase2 §2.4 + C8：messages 归一化是 Provider 实现细节（C8 70%/~150 行级）；phase2 §2.4 StreamRequest 直接接 `[]Message`，单一 canonical Message 类型。
**裁决：docs 错（C8 + R2 违反）**——双 Message 类型 = D1 反复反模式（与 AgentTool/ToolDefinition 同形）。

#### ⚠️-09.7 派生 — `providers/registry.go # sourceID for unregister`
**[§9.1 行 33]** ProviderRegistry 接口含 `unregister(sourceID)`。
phase2 §2.4：ProviderRegistry 接口仅 `Register(name, Provider) error` + `Get(name) (Provider, bool)`，无 Unregister。
**裁决：docs 错**——sourceID + unregister 是 pi extension hot-reload 支持机制；phase2 ProviderRegistry 是 Session 字段，Session 结束时所有 registration 随 Session GC 自然清除——unregister 是 pi-extension-system 的 leak。

### Scope 错位三项（原 ❌-09.2/4/5 重标，Owner 裁决 Q1 后归类⚠️）

> **Q1 Owner 接受 A5 自陈 self-doubt #2**：doc-09 §9.1 layout 自身未区分 tau 核心仓库 vs tau-based 产品仓库——doc 缺 scope 边界导致越界。**裁决方向不变**（这些子树不属 tau 核心模块），但归责从"docs 主动违规"调为"doc scope 模糊导致越界"。

- **⚠️-09.2-scope [行 56-72]** `tools/builtins/` (read/write/edit/bash/grep/ls) + `env/` (FileSystem + Shell + os/exec abort+timeout)
  phase2 §1：tau is not 产品。read/write/edit/bash/grep/ls = A2 §A.tools coding-agent 内建；env = 其执行环境抽象。**核心层只保 §2.6 Tool 接口，不带 builtins；不存在 env 包**。
  **注**：`bash.go` 注释"enforces multi-session git rules here"（行 60）独立构成 R4 违反（包级跨 session 状态）——即使 scope 边界画清后此条仍升 ❌。

- **⚠️-09.4-scope [行 81-84]** `skills/` 顶层包 (loadSkills + prompt_templates + frontmatter)
  phase2 §2.9 + §6 G3：不为 Skills 设计 first-class 抽象。独立顶层包 = first-class status，违反 G3。**核心层只保 §2.2 SystemPromptFn closure 入口；skills/ 整包归产品层**。

- **⚠️-09.5-scope [行 86-89]** `extensions/` 顶层包 (api + runtime invalidate + runner_starlark + runner_goja)
  phase2 + doc-06 整审：extension = 产品层。doc-09 §9.2 step 8 自标 OPTIONAL 已部分自我修正，但 §9.1 layout 仍列入 = scope 边界未画清。**核心层不含 extensions 包；产品层（如 coding-agent）自带 extension subsystem**。

### ➕ 补充项 (2)

- **[§9.2 行 134-141 build order 整体哲学]** \"validate the entire core+harness+compaction stack against the faux provider before writing a single byte of vendor-specific HTTP code\" — phase2 §5 给了 demo case 但**没明确给 build order**。**应补到 phase2 §7 Phase 4 PRODUCE 入口准备段**（去掉 harness / compaction 步骤，调整为 session-as-core / loop / faux / wire-protocol 顺序）。docs/09 给 tau 的最大正向贡献——**前提是把"harness/compaction"砍掉后**。
- **[§9.1 行 49-54 size table] LOC 估算（pi LOC × 1.3 = Go）** — 不入 phase2 主稿，但保留作 Phase 4 PRODUCE 输入；去掉 harness/compaction/skills/env 后 tau 核心估算 ≈ 4-5K LOC（core ≈ 3K + providers/faux ≈ 600 + providers/openai_completions ≈ 1.5-2K = 与 doc-09 §9.4 v0.1 估算 5K 接近）。

### ❌ 弃用项 (2，原 5 重标后)

- **❌-09.1 [§9.1 行 7] `core/types.go # AgentMessage, Tool, ToolResult, AgentContext`** — `AgentContext` 是 pi 的 ctx 概念延续。Go 已有 `context.Context` 标准抽象；引入 `AgentContext` = 命名碰撞 + pi 概念漂移。**任何叫 AgentContext 的核心 type 立即弃用**。
- **❌-09.3 [§9.1 行 65] `providers/openai_completions/compat.go # OpenAICompletionsCompat with all flags`** — 注释"with all flags"暗示 bag-of-flags struct → R1 violation 候选（公开 API 禁 `any` / type assertion）。**置信 50-60%**（A5 未读 pi 源码）。**弃用** Compat 类型设计；改为每 wire protocol 单独 Provider 实现 + 显式 ProviderOptions。

---

## §C — Q-First 3 答持久化

### Q1 — doc-09 scope ambiguity
**回答**：scope 错位（doc-09 自身没定清楚）。证据：§9.1 layout 把核心抽象（agent_loop / session）与产品层（builtins/skills/extensions/env）平铺为同级顶层包，**模块边界本身就没分核心 vs 产品**——doc-09 design 缺陷不是我读错。
**整合建议**：把 ❌-2/❌-4/❌-5 重标为 ⚠️ "scope 错位 — doc-09 未区分 tau 核心 vs 产品仓库布局"，**裁决方向不变**（这些子树不属 tau 核心模块），但归责从"doc 主动违规"调为"doc scope 模糊导致越界"。这种归类对 Phase 4 PRODUCE 更有用：v2 重写时第一刀是先画"core 仓库 vs product 仓库"边界。

### Q2 — ⚠️-1 派生症状合并 vs 拆开
**回答**：保留 5 个独立 ⚠️ + ⚠️-1 显式标注"根因，下列 ⚠️-3/4/5/6 + 部分 ❌ 是派生破坏面"。理由：(1) phase3 整合稿读者未必读过 doc-09 全文，单一根因 ⚠️ 会让破坏面规模看不清；(2) 每个派生症状各自带独立行号 + 独立 phase2 §X 引用，合并会丢精确 anchor；(3) Phase 4 PRODUCE builder 真改 v2 时是逐文件改，5 个独立 anchor 比 1 个抽象根因可执行性强。

### Q3 — A1 G4 evidence 命中后 ⚠️-4 维持还是降级
**回答**：⚠️-4 降级为 \"⚠️-pending-Phase 4 重审，Phase 3 不实质裁决\"，但 doc-09 ⚠️-4 anchor 不撤——理由从"L2 G4 已决策不重开"改为"G4 stated 理由 #2 已被 A1 evidence 证伪，决策实质暂悬空，doc-09 §9.1 行 22-27 + §9.2 step 5 的 compaction 子树作 evidence 保留等 Phase 4 重审使用"。

**A5 侧 evidence 支撑 A1**：
- doc-06 行 86 `pi.on(event, ...)` 明列 `session lifecycle (start/end/compact/...)`——pi extension API 一等支持订阅 `session:compact` 事件
- doc-06 行 92 `pi.registerProvider` runtime 注册 + 行 32 \"extension registers... providers\"——pi extension 一等支持注册新 provider
- 这两条 + A1 找到的 `examples/extensions/custom-compaction.ts`（gemini-flash do compaction）= **三角互证 pi 公开支持的扩展模式**：extension 注册 cheap provider → 订阅 session:compact → supply compaction via extension provider。**G4 stated 理由 #2 \"已知用户 0\" 不仅证伪，且证伪强度比 A1 单点 evidence 更高**（pi 把这模式做成了"公共 extension API + 官方 example"两个层面）。

**对 G4 重审的方向 hint**（不是 Phase 3 裁，仅作 v1.3 / Phase 4 输入）：A1 + A5 互证显示 compaction-via-extension-provider 是真实接入点而非过度抽象。重审时 G4 三理由（50 行成本 / 0 已知用户 / 与 Provider.Complete+Transcript 冗余）只剩理由 #1 + #3 立得住——而理由 #3"冗余"在 doc-06 行 86 + 行 92 evidence 面前也要重判。**所以 Phase 4 重审时 G4 实质问题不是 hook 该不该有，而是 phase2 §2.3 Transcript 是否暴露 "summarize → replace tail" 写入入口**。

---

## §D — A5 §5 Self-Doubt（强制 5 条）

1. **跳了什么**：phase2-naming-map.md / phase2-self-doubt.md / phase2-rule-enforcement.md / phase2-grey-resolutions.md（4 个 phase2 拆出文件）/ Phase 1 主稿 / pi 源码（按规约不读）/ docs/01–05/07/08（不在我审范围）。⚠️ 风险：phase2-rule-enforcement.md 可能含 lint 实现 PoC 细节。
2. **假设了什么**：(a) 假设 phase2 §1 \"core 包内禁具体 provider 名和 tool 名\" 也覆盖**模块布局推荐文档中列举的子包名**——可能过严；(b) 假设 doc-09 是给 tau 核心而非整个 tau-based 产品仓库的模块布局——⚠️-medium-confidence（Q1 已自我修正：doc-09 自身 scope 错位）。
3. **受 docs 精心写作说服的可能性**：(a) doc-09 §9.3 \"validate against faux first\" 段写得非常有说服力——build order 哲学独立于具体包布局，我警觉到此后才把 ✅ 和 ⚠️ 拆开；(b) doc-06 §6.5 stale-ctx 段 \"Invalidate-and-throw beats silent staleness\" 写得极简极有力——Owner 提前裁决 Phase 5+ trigger 救了我。
4. **没把握的判断**（标 ⚠️-low-confidence）：
   - doc-09 ❌-3 (Compat any flags) 置信 50-60%（未读 pi 源码也未读 Phase 1 主稿 D13 完整段）
   - doc-06 ⚠️-3 (`pi.on` chain-of-responsibility) 置信 ~75%
   - doc-09 ⚠️-7 (sourceID unregister) 置信 ~70%
5. **未走完 Five Gates 的项**：上述 ⚠️-low-confidence 三条 = Gate 1（PROOF）部分依赖 doc 文字未直查源码——按规约不查 pi 源码，标置信度而非升级。

---

## §E — A5 状态总结

- ✅ doc-06 + doc-09 主审完成
- ✅ Q-First 3 答 + 三角互证发现（doc-06 行 86 + 行 92 升级 A1 G4 evidence）
- ✅ §5 self-doubt 5 条（含 4 个低置信项透明标注）
- ✅ scope 错位三项重标 + ⚠️-06.3/06.4 evidence 升级版 fragment（已交 coord-audit）
- 预算使用：~36/80 turn（远低于上限）
- **二选一裁决树执行**：L1 ~85% / L2 ~15% / L3 = 0 / L4 = 0 — 0 deadlock 0 escalate

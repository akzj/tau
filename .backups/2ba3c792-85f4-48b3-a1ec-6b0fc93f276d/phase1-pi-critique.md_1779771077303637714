# Phase 1 — pi 批判性架构分析（整合稿）

> **本文档是整合稿（synthesis），不是子报告的拼接。**
> 子报告原文：[`phase1-pi-critique-A1.md`](./phase1-pi-critique-A1.md)（packages/agent，509+34 行）、[`phase1-pi-critique-A2.md`](./phase1-pi-critique-A2.md)（packages/ai + coding-agent 抽样，609 行）。
> 本文档负责：① 鸟瞰 ② 跨切面发现（两份报告独立印证或互补的结论）③ Criterion A 跨语言移植评估 ④ Criterion B 模式门槛判定 ⑤ 合并盲区 ⑥ 方法论说明 ⑦ Phase 2 入口约束。
> 单点结论的证据请翻子报告。

---

## 0. 方法论说明

### 团队组成与分工
- **Coordinator (我)**：决策方法论、设计 question-first 挑战、整合、不读 pi 源码（防止与 analyst 重叠消耗 context）。仅读 `pi/AGENTS.md` 173 行（小、给作者声音）。
- **A1 — agent-core analyst**（read-only）：`pi/packages/agent/`（src 8k 行 / 25 文件 + docs/ + AGENTS.md），重点完读 5 个核心文件 ~3.4k 行。
- **A2 — provider-tools analyst**（read-only）：`pi/packages/ai/`（30k 行抽象核心完读 + 1 provider 抽样）+ `pi/packages/coding-agent/` 轻抽样（tool 协议 + skills 机制）。

### 流程
1. 体量摸底（`find` + `wc`）→ 确定取样边界，**不全读 96k 行**
2. 制定 6 条硬准则（可测试性 / 可演化性 / 抽象不漏 / 隐式状态 / 错误路径地位 / 并发会话隔离）+ 「类型在撒谎」红旗 + 三件套证据格式（path:line + ≤8 行片段 + 决策命名）
3. 子报告交付时**不直接接受** → Question-First 协议：每份子报告抽 4-5 个最弱论点要求二次取证
4. 子报告作者据 Q-First 反馈**自行修订并 update 主文件**（A1 自我修正 1 处、A2 自我修正 2 处 + 新增 1 处 meta-debt 证据）
5. 我做 synthesis：跨切面、Criterion A（TS-bound vs Go-port）、Criterion B（≥3 实例才能称模式）、合并盲区

### 红线（贯穿全程）
- ❌ 不信 `tau/docs/01..09.md`——它们是别人的分析、可能带 pi 视角偏见。两位 analyst 自陈"完全没读"。
- ❌ 不写代码、不模板化结论、不默认 pi 是基线
- ✅ 第 5 节"我没读什么 / 我假设了什么 / 我哪里没把握"是强制护栏（owner 钦点）

### 取样而非全读的取舍
- 完读：8k agent core / 30k ai 抽象核心 / coding-agent 的 tool+skills 子系统 / AGENTS.md
- 抽样：1 个 provider 适配器（anthropic 代表）/ test 体感（grep 统计 + 头部抽样）
- 跳过：4 个 provider 的具体协议适配 / TUI 全部 / coding-agent 产品逻辑（CLI 解析 / UI / business logic）/ branch-summarization 内部

跳过理由：tau 的范围（parent 给的 working assumption）= **agent core + provider 抽象**，不做 coding-agent 类产品、不做 TUI。所以 ~58k 行的产品+UI 代码不在批判目标内。

---

## 1. pi 鸟瞰

### 1.1 形态
pi 是 TypeScript monorepo，4 个 npm package，约 96k 行 src（不含 test）：

```
应用层    │  pi-tui (11k)         ← 终端 UI (tau 不涉及)
────────  │  pi-coding-agent (47k) ← 把 agent 做成 Claude Code 类产品 (tau 不涉及，仅取协议)
抽象层    │  pi-agent (8k)        ← agent loop / harness / session / compaction / skills
基础层    │  pi-ai (30k)          ← provider 抽象 / streaming / 消息归一化 / 工具协议适配
```

**单 monorepo 一次发版（lockstep versioning，由 AGENTS.md 第 6 节定义）**。

### 1.2 核心抽象（按层）

**pi-ai 层**：
- `StreamFunction = (Model, Context, Options) → EventStream` —— provider 的本质是个**纯函数**（`pi/packages/ai/src/types.ts:191-199`），不是注入 HTTP client 的对象
- `apiProviderRegistry: Map<string, RegisteredApiProvider>` —— 全局模块级 Map（`api-registry.ts:38`），启动注册 + extension 运行时注册
- `transformMessages(...)` —— 6 个 provider 在 stream 入口都调，做图像降级 / 孤儿 toolCall 修补 / errored assistant 跳过 / 跨 provider thinking 转 text（`transform-messages.ts` 220 行）
- `EventStream`：双消费协议（`for await` + `await result()`），状态机封装在 `utils/event-stream.ts`
- `Faux provider`：通过**真注册路径**进入（`faux.ts:339`），测试**不 mock 网络**，与真 provider 同代码路径

**pi-agent 层**：
- `runAgentLoop(prompts, context, config, ...)` —— 低层 loop，入口处 spread copy `messages` 进入内部 mirror（`agent-loop.ts:95-107`）
- 两套并列高层封装（**这是核心债 #1**）：
  - `Agent`（`agent.ts:557 行`）—— 一个状态封装
  - `AgentHarness`（`harness/agent-harness.ts:995 行`）—— 另一个状态封装 + 持久化 session 树 / hooks / compaction 编排
- `Session = Tree`（leaf + parentId + branch_summary + compaction entry，`harness/session/session.ts:252 行`）—— 不是 list，是 tree
- `Result<T,E>` + 6 个稳定 error code class —— 错误是 first-class 值
- `Hooks`：`emitHook<T>` (last-wins) 和 `emitBeforeProviderRequest` (chain) 两种语义共存于同一个 emit 家族（**核心债 #5**）

**pi-coding-agent 层**（仅取协议形状）：
- `ToolDefinition`（`extensions/types.ts:426-475`）—— 12 个字段（5 required + 7 optional），承担 LLM 协议 / UI render / prompt meta / 执行模式四种语义
- `Skills` —— `SKILL.md` 文件 + frontmatter，**LLM 自主 read**，不是 plugin

### 1.3 作者声明 vs 实际代码（meta 观察）
- **AGENTS.md 没有架构/设计原则章节** —— 只是 contributor 操作手册（git/release/lockfile/changelog）。设计意图散落在 `packages/agent/docs/` 和代码内。
- **作者自定红线被作者本人主动绕过**（这是 §4 meta-debt 的核心证据）：
  - AGENTS.md:9 "No `any` unless absolutely necessary" ⇄ `biome.json:13 "noExplicitAny": "off"`（**主动关掉 lint**）
  - AGENTS.md:14 "No inline imports" ⇄ `register-builtins.ts` 9 处 TC39 `await import()`，无文档化豁免
- "Multiple pi sessions may be running in this cwd at the same time" —— 这是 AGENTS.md Git 章节的字面声明，**multi-session-safety 是已知运行场景**，不是理论需求。这让 §3 中"全局 Map 跨 session 互见"的债更严重。

---

## 2. 真正值得借鉴（必抄清单 + 跨语言移植评估）

> 每条挂 6 准则中的至少 1 条 + Criterion A 移植评估（TS-bound 风险、Go 移植路径、是否需重设计）。
> 子报告中已被两位 analyst 独立提及的设计 = 强信号，但仍需 **Criterion A** 二次审视——独立性受限于"两人都看 pi"。

### B1 — Stream-as-pure-producer（provider 是函数，不是对象） ⭐⭐⭐
**是什么**：provider 的形态是 `StreamFunction = (Model, Context, Options) → EventStream`（`packages/ai/src/types.ts:191-199`），不是"注入 HTTP client 的 ProviderClient 对象"。Loop 也通过 `streamFn` 注入接受任意 producer。
**为什么真好**：
- 准则 1（可测试性）：`packages/agent/test/harness/*` **0 处 vitest mock**，全用 `streamFn` + faux 注册（A1 优点 1 + A2 优点 1+2 互证）
- 准则 3（抽象不漏）：使用者不需要懂 SSE / OpenAI streaming JSON / Anthropic event format，只面对 EventStream
- 准则 4（隐式状态）：纯函数没有藏单例
**Criterion A**：
- TS 实现机制：async generator + `for await...of`
- Go 移植路径：`type StreamFunc func(ctx, model, opts) <-chan Event` 或 `iter.Seq[Event]` (Go 1.23+) 或自定义 `Iterator`
- **核心思想跨语言成立**（provider = producer function）；机制改用 channel/iter 即可
- ⚠️ TS 的 async generator 给"暂停/恢复"的语法糖，Go 的 channel + goroutine 等价但要手动处理生命周期 / 关闭信号 / 错误传播——**实现复杂度高于 TS**
- **结论**：必抄，但实现重做（generator → channel/iter）

### B2 — Faux provider 走真注册路径 ⭐⭐⭐
**是什么**：测试用的 faux provider 通过**真正的 `registerApiProvider`** 进入系统（`packages/ai/src/test/faux.ts:339, 470, 496`），不是 mock，也不旁路任何代码路径。
**为什么真好**：
- 准则 1（可测试性）：测试用的 provider 走和生产 provider 完全一致的注册/解析/调用代码路径——测试覆盖到的就是真路径
- 准则 5（错误路径）：错误注入也通过 faux 的"返回错误事件"完成，错误处理在测试中验证
**Criterion A**：与语言无关，是注册机制设计。
- **结论**：必抄，无障碍

### B3 — Result<T,E> + 稳定 error code ⭐⭐
**是什么**：`packages/agent/src/harness/types.ts` 定义 `Result<T, E>` discriminated union + 6 个 error class，每个有 stable `code` 字段。错误是返回值不是抛出。
**为什么真好**：
- 准则 5（错误路径）：错误是 first-class 值，调用方静态可知有哪些错误形态
- 准则 1（可测试性）：错误路径可被断言，不需要 try/catch
**Criterion A**：
- Go idiom 本就是 `(T, error)` + `errors.Is/As` + 自定义 error type with `Code()` method
- **结论**：必抄、Go 比 TS 更自然

### B4 — Session 是 tree，不是 list ⭐⭐
**是什么**：`packages/agent/src/harness/session/session.ts:252` —— session 数据结构包含 `leaf` + `parentId` + `branch_summary` + compaction entry，不是线性历史。
**为什么真好**：
- 准则 2（可演化性）：支持 branch / fork / compact 而不丢历史
- 准则 5（错误路径）：失败的 turn 可作为 branch 保留，不污染主线
**Criterion A**：纯数据结构选型，与语言无关。
- **结论**：必抄，无障碍

### B5 — Compaction prepare/execute 拆分 ⭐⭐
**是什么**：`packages/agent/src/harness/compaction/compaction.ts:755` 的 compaction 主流程明确拆成 `prepare()`（决策"哪些消息要被压缩"）+ `execute()`（实施压缩）两段，hook 可整段替换 execute。
**为什么真好**：
- 准则 2（可演化性）：策略可替换（手动 / LLM 总结 / 重写历史 / 第三方）
- 准则 1（可测试性）：prepare 可纯函数测，不起 LLM
**Criterion A**：结构性模式，跟语言无关。
- **结论**：必抄 ⚠️ 见 [handoff §2.2](./phase1-to-phase2-handoff.md#22-待验证必抄phase-2-必须先做行为验证才能落地-)：`prepare/execute` 拆分**机制证据**充分（A1 验证 prepare 可纯函数测），但 **execute hook 整段替换的真实场景未观察到**。Phase 2 设计 compaction 系统时需先确认是否真有 ≥2 种 execute 实现需求；否则 hook 替换可能是过度抽象。

### B6 — 零 process.env in src（Node 特殊 API 隔离到 env/nodejs.ts） ⭐⭐⭐
**是什么**：`grep "process\.env" packages/agent/src/` 排除 `harness/env/nodejs.ts` 后 **0 命中**（A1 已验证）。
**为什么真好**：
- 准则 4（隐式状态）：核心抽象**没有任何隐式环境依赖**——所有"运行环境"通过 `ExecutionEnv` interface 注入
- 准则 6（并发隔离）：不同 session 可注入不同 ExecutionEnv（不同 cwd / env / spawn 策略）
**Criterion A**：
- Go 等价：用注入的 `Env interface { Getenv(string) string; Cwd() string; ... }`，禁 src 直接 `os.Getenv`
- **结论**：必抄、Go 比 TS 更自然（Go 没有 `process.env` global，禁起来更容易）

### B7 — 并行 tool 执行的确定性顺序 ⭐⭐
**是什么**：`packages/agent/src/agent-loop.ts` 的 parallel tool execution 严格区分"completion 顺序"和"source 顺序"，returned slice 按 source order 索引，不按完成早晚。
**为什么真好**：
- 准则 6（并发）：concurrent execute + deterministic ordering = 既快又可重现
- 准则 1（可测试性）：测试不需要排序断言
**Criterion A**：
- Go 等价：`errgroup.Group` + index-stable result slice
- **结论**：必抄

### B8 — Skills 是文件约定，不是 plugin 系统 ⭐
**是什么**：`packages/coding-agent/src/.../skills.ts:308-340` —— Skills = `SKILL.md` 文件 + frontmatter（`name` / `description`），LLM 通过 tool 自主 `read` 触发，**不是注册回调机制**。
**为什么真好**：
- 准则 2（可演化性）：加 skill = 加文件，不需要改代码
- 准则 3（抽象不漏）：skill 作者写 markdown，不需要懂 agent 内部
**警告（A2 §5 自陈）**：这条评价是**设计判断而非行为数据**——A2 没观察到 LLM 实际使用 skills 的体感，只看了机制。
**Criterion A**：与语言无关，是文件协议设计。
- **结论**：必抄，**但需要 Phase 2 时回头验证 LLM 行为**（不是机制本身的问题，是机制的有效性证据不足）

### B9 — TypeBox 三合一：TS 类型 = JSON Schema = 运行时校验 ⭐⭐
**是什么**：`packages/coding-agent/src/.../tools/read.ts:21` 等使用 TypeBox 定义 `Type.Object({...})`，**同一个表达式**生成 TS 类型、JSON Schema（给 LLM 的 tool params）、运行时验证函数。
**为什么真好**：
- 准则 3（抽象不漏）：tool 定义只写一次 schema
- 准则 5（错误路径）：运行时拒收错误的 tool args 在抽象边界完成
**Criterion A**：
- ⚠️ **HIGH 移植风险**：TypeBox 本质是 TS 类型系统的 reflection trick（用 type-level computation 从 schema 推导 TS 类型）
- Go 没有等价语言机制。可选三条路：
  1. `reflect` + jsonschema 库（运行时反射，Go 写起来啰嗦）
  2. Schema-first codegen：写 schema → 生成 Go struct + validator
  3. Struct tag + 手写 validator（最 Go-native，但 schema 来源不单一）
- **结论**：**思想必抄（"schema 单一来源"），机制必须重设计**。Phase 2 设计时需要专门决策走哪条路。

### B10 — EventStream 双消费（`for await` + `await result()`） ⭐
**是什么**：`packages/ai/src/utils/event-stream.ts` 的 EventStream 同时支持 `for await...of stream`（消费每个 event）和 `await stream.result()`（拿终态），同一对象。
**为什么真好**：
- 准则 2（可演化性）：消费者可选"我只要终态"或"我要每个 token"
- 准则 1（可测试性）：测试可以只 await result() 不订阅 stream
**Criterion A**：
- ⚠️ **MEDIUM 移植风险**：依赖 TS 的 async iterable + Promise 双协议同对象
- Go 等价：channel + final-result accessor function（不是同一对象）；或 context.Context 风格的"完成时 close + 结果可读"
- **结论**：思想必抄（流 + 终态同时可达），机制不同

---

### 必抄汇总

| # | 设计 | 准则覆盖 | 移植风险 | 行动 |
|---|---|---|---|---|
| B1 | Stream-as-pure-producer | 1,3,4 | LOW (机制重做) | 必抄，generator→channel |
| B2 | Faux 走真注册路径 | 1,5 | NONE | 必抄无障碍 |
| B3 | Result+stable codes | 1,5 | NONE (Go 更自然) | 必抄 |
| B4 | Session-as-tree | 2,5 | NONE | 必抄 |
| B5 | Compaction prepare/execute split | 1,2 | NONE | 必抄 ⚠️ 见 handoff §2.2：execute hook 整段替换的真实场景未观察到 |
| B6 | Zero process.env in src | 4,6 | NONE (Go 更自然) | 必抄 |
| B7 | 并行 tool 确定性顺序 | 1,6 | LOW | 必抄 |
| B8 | Skills as files not plugins | 2,3 | NONE | **待验证必抄** — 见 [handoff §2.2](./phase1-to-phase2-handoff.md#22-待验证必抄phase-2-必须先做行为验证才能落地-)。机制审视显示优雅，但**LLM 行为数据缺失**（A2 §5 自陈），不能直接进设计稿 |
| B9 | TypeBox tri-unity | 3,5 | **HIGH** | 思想必抄，机制 Phase 2 决策 |
| B10 | EventStream 双消费 | 1,2 | MEDIUM | 思想必抄，机制不同 |

---

## 3. 必弃清单（按严重度排序）

### D1 [严重] 双重抽象漂移（dual-abstraction drift）
**实例**：
- `Agent` (557 行 / `agent.ts`) vs `AgentHarness` (995 行 / `harness/agent-harness.ts`)：两套并列高层封装，都包 `runAgentLoop`，行为漂移、bug 修一漏一（A1 §3.1）
- `AgentTool` ↔ `ToolDefinition` 双类型，`agent-session` 维护并行 `_toolRegistry` / `_toolDefinitions` 两份 Map (`coding-agent/.../agent-session.ts:300-320`，A2 §3.6）

**A1 自验证（Q1）**：Agent 和 AgentHarness 都不绑 Node、import 树都干净——**不是 browser/Node 故意分层**，是纯并列冗余。

**为什么严重**：
- 准则 2：演化时改一处忘一处
- 准则 6：维护双 source of truth 永远会漂移

**tau 必须**：**一个概念一个 canonical 抽象**。设计时禁止"再加一个相邻类"作为演化路径——只能 refactor 现有类或显式分离正交关注点。

**Criterion B**：2 个独立实例。**不到 3 实例门槛 → 降级为"反复出现的反模式（2 例已确认）"**，非"系统性"。

### D2 [严重] 类型在撒谎（types lying）
**实例（≥3 不同模块）**：
- `agent-harness.ts:701, 759, 774`：3 处 `new AbortController().signal` 立即丢 controller，signal 永不 fire。**第 3 处更严重**——传给 `generateBranchSummary` 这个真发 LLM 请求的函数，意味着 branch summary 生成**不可被外部中断**（A1 §3.3 + Q3 升级）
- `packages/ai/src/providers/openai-completions.ts`: 20+ 处 `as any`；anthropic 3 处；google 4 处（A2 §3.3）。典型例：`parameters: tool.parameters as any  // TypeBox already generates JSON Schema`（`openai-completions.ts:996`）
- `harness/types.ts` 的 `createErrorToolResult` 把 `details: TDetails` 偷换成 `{}`（A1 §3.7）

**为什么严重**：
- 准则 3（抽象漏）：调用者按类型契约写代码，运行时行为不一致
- 准则 5（错误路径）：基于"signal 会 fire"的清理逻辑永不执行 = 静默资源泄漏

**tau 必须**：类型签名 = 运行时真相。禁 `as any`/`interface{}` 假饰；签名说能 cancel 就必须真能 cancel。

**Criterion B**：≥3 独立实例分布在不同模块 → **模式门槛满足**。

### D3 [严重] meta-debt：纸面纪律 vs 自动化执行的系统性脱钩 🚩
**实例**：
- AGENTS.md:9 "No `any` unless absolutely necessary" ⇄ `biome.json:13 "noExplicitAny": "off"` —— **作者主动关掉对应 lint**。后果：src 共 41 处 `as any`（A2 Q5）
- AGENTS.md:14 "No inline imports" ⇄ `register-builtins.ts` 9 处 TC39 `await import()`，无文档化豁免（A2 §3.4 + Q4）
- 其他可推断：`packages/agent/docs/agent-harness.md` 自陈 phase state machine 有 todo 项（"retry"、"Audit/Finalize/Implement"），但代码并未推进，docs 和代码漂移（A1 §3.6）

**为什么严重**：
- 准则 4（隐式）：规则只在文档存在 = 不存在；新加贡献者无法靠工具学习边界
- 准则 2（演化）：规则会随时间继续退化，技术债会**继续累积**因为没人会被自动拦下
- **元层面**：这是"项目自治体的纪律 vs 工具链强制"的脱钩。不是单条 bug，是**生产 bug 的机制**。

**tau 必须**：每条红线绑一条自动化检查（Go 生态：`golangci-lint` 自定义 linter / `go vet` analyzer / CI grep gate）。**写在 docs 但没 lint 兜底的红线视为不存在**。

**Criterion B**：3 个独立实例 → 模式门槛满足。

### D4 [严重] 全局 mutable Provider Registry（multi-session-safety 反例）
**实例**：`api-registry.ts:38` 定义模块级 `apiProviderRegistry: Map<string, RegisteredApiProvider>`。
- 启动时 read-once：`register-builtins.ts:346-394`（OK）
- 测试时 randomId 命名空间隔离 + tearDown：`faux.ts:470, 496`（OK）
- **runtime extension 调用**：`coding-agent/src/core/model-registry.ts:873` 经 `pi.registerProvider(name, config)` 写到**同一全局 Map**
- 作者注释（model-registry.ts:790-795）："Register a provider dynamically (from extensions). If provider has models: replaces all existing models for this provider..." —— **作者明知 mutable，但没设 scoped registry API**

**为什么严重**：
- 准则 6（多 session 隔离）：AGENTS.md 字面声明"Multiple pi sessions may be running in this cwd at the same time"——**已知运行场景**。同进程多 session 时 session A 的 extension 注册的 provider 立即对 session B 可见，**不是 feature 是 bug**。
- 准则 4（隐式）：跨 session 状态共享不在类型签名里

**tau 必须**：**Registry 实例化**，绑 session（或 workspace）作用域。运行时 mutable registration 只能影响当前 session，不能影响兄弟 session。

### D5 [中] Hooks 系列两套语义共存（last-wins vs chain）
**实例**：
- `emitHook` (last-wins, `agent-harness.ts:230-242`)：循环里**不**把上一轮 result 传给下一轮，最后一个非 undefined 返回值赢
- `emitBeforeProviderRequest` (chain, `agent-harness.ts:250-264`)：累积 patch (`current = applyStreamOptionsPatch(current, ...)`)
- 同一 emit 家族同一 API 命名，但语义按 event type 暗分两套，**调用者无法静态判断**

**A1 Q4 给的踩坑场景**：使用者注册两个 `on("context")` handler——A 注入 README 头部、B 截断超长 history。直觉以为 chain（因为 hook 名叫 "transform"），实际是 last-wins，A 的注入**完全丢失**。

**为什么严重**：
- 准则 3（抽象漏）：API 形态相同、行为不同 = 调用者必读源码
- 准则 5（错误路径）：错误模式是**静默丢失**，不是抛错

**tau 必须**：**两种语义两个 API**。命名上区分（`reduceFirst` vs `chain`）；类型签名上区分（last-wins 返回 `Result | undefined`，chain 返回 `Result`）。**绝不允许同一 API 按参数暗分语义**。

### D6 [中] Phase 状态机未收敛
**实例**（`harness/agent-harness.ts` + `harness/types.ts:485`）：
- 类型定义 `AgentHarnessPhase = "idle" | "turn" | "compaction" | "branch_summary" | "retry"`
- 9 处 `this.phase = ...` 赋值实际只用 4 个值，**`"retry"` 0 处赋值**（A1 Q2 grep 验证）
- `docs/agent-harness.md:97` 自承"Implement retry handling" 在 todo

**为什么严重**：
- 准则 4（隐式）：类型说有 5 个 state，运行时只有 4 个 = 类型在撒谎（D2 的子实例）
- 准则 5：abort 事件晚于 agent_end，phase 退出顺序未定义

**tau 必须**：状态机的所有状态必须有进入路径 + 退出路径，**类型定义就是规约**。CI 检查"枚举值是否都有 producer/consumer"。

### D7 [中] Loop 内部 transcript 隐式契约（loop-internal-mirror-is-invisible-exhaust）
> 注：A1 自我修正过的债。原版"loop mutate 调用方 messages"是错的，但底层问题确实存在——形式不同。

**实例**（A1 §3.2 修正版）：
- `runAgentLoop:95-107` 入口做 spread copy `messages: [...context.messages, ...prompts]` 进入内部 `currentContext`
- streaming 期间 push partial / splice replace final 全在内部副本上（`agent-loop.ts:317, 333, 346, 348, 361, 363`）
- 这份内部 mirror **不通过返回值暴露**（`runAgentLoop` 返回 `newMessages` 是平行收集的）
- **唯一被读的地方是下一 turn 的 `transformContext` 输入**（`streamAssistantResponse:283`）
- 调用方（`Agent` 类）必须独立 subscribe events 重建自己的 transcript（`agent.ts:489 _state.messages.push(event.message)`）

**为什么严重**：
- 准则 4（隐式）：两份 transcript，其中一份隐式存在但实际是 transformContext 的输入。类型/文档没声明这件事。
- 准则 2（演化）：将来想让 transformContext 看到调用方持久化层的 transcript，没干净 hook 点

**tau 必须**：**单一 transcript source of truth**。loop 不维护内部 mirror；transformContext 的输入显式由调用方注入的 `Transcript` interface 提供。Go：`type Transcript interface { Messages() []Message; Append(Message) }`。

### D8 [中] Agent.prepareNextTurn 比 loop 接口信息更少（向上漏底）
**实例**（A1 §3.4）：`Agent.prepareNextTurn` 的回调签名比底层 `runAgentLoop` 的 `transformContext` 少了 `message` / `toolResults` / `context` —— 高层比低层信息**更少**。
**为什么是债**：
- 准则 3（抽象不漏）：高层应该信息**等量或更丰富**，不能更少。否则使用者要"反向降回低层"才能拿到必要信息。
**tau 必须**：高层 hook 必须 ≥ 低层信息量。

### D9 [中] 双重 bookkeeping streaming
**实例**（A1 §3.8）：streaming message 既被 loop push 到 `currentContext.messages`、又通过 events emit 给外部 state。两份独立 bookkeeping，调用方漏 subscribe 一个事件类型，他的 transcript 就和 loop 内部不一致。
**与 D7 关系**：D7 是病因（loop 自己记一份），D9 是病象（两份不一致）。
**tau 必须**：见 D7。修了 D7 就消了 D9。

### D10 [中] OpenAICompletionsCompat 18 字段开关
**实例**（A2 §3.2）：`packages/ai/src/types.ts:319-372` 有个 `OpenAICompletionsCompat` interface，18 个 boolean / string 字段，每个对应一个"OpenAI 兼容厂商的方言"。每加一个兼容方就改这个共享 type。
**为什么严重**：
- 准则 2（演化）：兼容厂商无穷无尽，type 会无限膨胀
- 准则 3（抽象漏）：用户要写一个 OpenAI 兼容客户端，必须知道 18 个开关的含义
**tau 必须**：兼容差异通过**独立的 provider 实现 + 共享 helper 函数**承载，不通过共享 type。即"加方言加文件，不加字段"。

### D11 [中] ToolDefinition 模块图耦合
> 注：A2 已 Q3 自我修正——从重度降到轻度。

**实例**（A2 §3.5 修正版）：`extensions/types.ts:31-43` 的模块级 type import 把 `pi-tui` 和 `modes/interactive/theme/theme.ts` 拖进 ToolDefinition 的依赖图。LLM-only tool 即使不 render UI，模块图仍 transitively 拉 TUI 类型。
**为什么是债**：
- bundler tree-shake 失效
- LLM-only context（如 server-side / no UI）也得带 TUI 依赖
**tau 必须**：分离三个类型——`ToolSpec`（LLM 协议）/ `ToolPromptMeta`（system prompt 注入）/ `ToolRenderer`（UI）。LLM-only tool 只写 ToolSpec。

### D12 [中] 工厂函数手抄重复（7 个 createTool/createAllTools/...）
**实例**（A2 §3.7）：`packages/coding-agent/src/.../tools/index.ts` 有 7 个手写组合工厂（createCodingTools / createAllTools / ...）。加一个 tool 改 7 处。
**为什么是债**：
- 准则 2（演化）：tool 注册不是一次声明，而是要"找到所有 factory 加进去"
- DRY 违反
**tau 必须**：**单一注册点**——tool 定义自带"所属 group / capability"标记，工厂从注册表过滤产生，而不是每个工厂手枚举。

### D13 [中] 产品决策污染 provider 抽象
**实例**（A2 §3.8）：`packages/ai/src/providers/anthropic.ts:69-89` **硬编码 17 个 Claude Code 工具名**；`isOAuthToken` 时强制重命名工具。**这是 product layer 决策（Claude Code 是 pi 的产品包装）侵入 provider abstraction layer**。
**为什么严重**：
- 准则 3（抽象漏）：provider 抽象层泄漏产品形态
- 准则 2（演化）：换产品就要改 provider 适配
**Criterion B**：1 个实例 → 不到 3 实例门槛 → "单点错误"非"系统模式"。但**这 1 个就足够严重**因为它在最该干净的层（provider 适配）。
**tau 必须**：provider 抽象层禁止知道任何产品名 / 任何具体 tool 名。如果 provider 需要按 tool name 做特殊处理，必须以"调用者注入的策略"形式（如 `transformToolCall(toolName) → toolName`）。

### D14 [轻] process.env 直读 in provider
**实例**（A2 §3.9）：`packages/ai/src/providers/anthropic.ts:47` 直读 `process.env.X`。违反 B6 的隔离原则。
**Criterion B**：相对于 B6 的"全 src 0 处" 这是**单点违例**，pi-agent 干净、pi-ai 漏了。
**tau 必须**：env 注入贯穿到 provider 层，不允许 provider 直读 env。

### D15 [轻] 合成字符串注入作 LLM 行为线索
**实例**（A2 §3.10）：`transform-messages.ts:142-184` 在 messages 里注入合成字符串"No result provided" + `isError: true`。LLM 行为开始依赖这个字符串怎么解读。
**为什么是债**：
- 准则 5（错误路径）：错误信号通过字符串约定传递，不是结构化字段
**tau 必须**：错误状态用结构化字段表达，不通过 LLM 看得懂的字符串约定。

---

## 4. 灰色地带（双面性 / Trade-off / 留给 Phase 2 决策）

### G1 — Mid-session provider 切换（A2 §4.4，已修正版）
- **优面**：用户可在同一 session 中切 model（GPT → Claude），有连续性
- **缺面**：`transform-messages.ts` 220 行代码维护跨 provider 兼容（thinking 跨模型转 text、toolCallId normalize），增加心智负担
- **A2 修正版的关键数据**：220 行中只 30%（~50 行）是切 provider 专用，70%（~150 行）是**同 provider 内必需归一化**（图像降级 / 孤儿 toolCall 修补 / 跳过 errored assistant）—— 6 个 provider 在 stream 入口都调，与是否切 provider 无关
- **tau Phase 2 决策**：建议禁用跨 session provider 切换（节省 ~50 行复杂度），但**归一化层 ~150 行必须保留**

### G2 — Skills 自主 read vs 强制注入
- **优面**：LLM 自主选择需要的 skill = 上下文节省
- **缺面**：依赖 LLM 决策能力——若 LLM 不主动 read，关键 skill 永不生效
- **A2 §5 警告**：B8 的"好"是机制判断，缺少 LLM 实际行为数据
- **tau Phase 2 决策**：默认自主 read，frontmatter 加 `inject: always` 选项让作者 opt-in 强制注入

### G3 — 函数式 vs 接口式 provider shape
- **优面（函数式）**：纯函数最易测、最易组合、最易 streaming → B1
- **缺面（函数式）**：横切关注点（auth、retry、quota、observability）只能通过装饰器嵌套，签名复杂
- **接口式 trade-off**：`type Provider interface { Stream(...); Capabilities(); ... }` 更清晰但失去函数纯度
- **tau Phase 2 决策**：建议主体函数式（B1），但提供"标准 Provider builder"模式封装常见装饰器栈

### G4 — Compat 字段化 vs 独立 provider
- **优面（字段化，pi 当前）**：兼容 OpenAI 系厂商写一个 type 就够
- **缺面**：D10——18 字段无限膨胀
- **trade-off**：独立 provider 文件多但每个干净
- **tau Phase 2 决策**：中间路——共享 helper 函数（如 `buildOpenAICompletionsRequest`），不共享 type；每个 provider 独立组合 helper

### G5 — Declaration Merging（A1 §4.2）
- **TS 特性**：使用者可对 pi 声明的 type 做 declaration merging 扩展（如自定义 `EventTypeMap`）
- **优面**：扩展类型不需改 pi 源码
- **缺面**：Go 没有 declaration merging
- **tau 决策**：**直接放弃**——Go 用 generic + 类型参数做"扩展点"，不学这个 TS-only 设计

### G6 — TypeBox 锁定（A1 §4.3 + B9）
- **优面**：B9 的三合一非常 powerful
- **缺面**：与 TypeBox 绑死，换 schema 库要改 ToolDefinition 结构
- **tau Phase 2 决策**：见 B9 移植评估——**思想必抄、机制重设**，不可直接平移 TypeBox

---

## 5. 合并盲区（merged §5 self-doubt）

### Coordinator (我) 的盲区
- **未做 cross-system 调研**：Criterion A 的"TS-bound vs 跨语言"判断基于我对 TS 和 Go 的语言知识，**没有调研其他 agent harness**（如 LangChain / openai-agents-python / AutoGen）来验证 pi 的设计是不是"业内常见好实践"还是"pi 独到"。这意味着 B1-B10 中可能有项目其实是 pi 独到的不一定对。
- **没读 docs/01..09**：刻意为之，避免污染。但也意味着如果 docs 有我们都没看到的关键 insight，整合稿不会包含。Phase 3 audit docs 时再处理。
- **tau 目标语言假设 = Go**：基于 docs/09 标题"go-module-structure"和 parent 给的 working assumption，**用户未亲口确认**。如果 tau 是 Rust / Python / TS-but-stricter，§2 整张移植表要重做。
- **AgentTool 形状跨范围未读**：A1 范围有 Agent + AgentHarness 但没专门讲 AgentTool 类型形状；A2 接触了 AgentTool 但只在 coding-agent 调用层。AgentTool 本身的定义文件 + 与 ToolDefinition 的转换逻辑（tool-definition-wrapper.ts）没人完整读过。**Phase 2 设计 tool 协议时可能要补这一块**。
- **D1 dual-abstraction 仅 2 实例**：按 Criterion B 我已降级为"反复出现的反模式"非"系统性"。如果将来发现第 3 实例（如 Storage 接口的双实现），可升回 pattern。

### A1 (agent-core) 的盲区
- 60% 把握的结论已 Q-First 验证 4/5 站住、1/5 自我修正
- 跳过了 `proxy.ts` 大部分（367 行）、`storage` 实现细节、`branch-summarization.ts`
- 没读全部 test 文件，仅头部抽样 + grep 统计

### A2 (provider-tools) 的盲区
- 跳过了具体 provider 1212 行细节、`agent-session.ts` god class 整体、`extensions/runner`、`modes/*`
- B8（Skills 是好的）评价基于机制审视，没有 LLM 行为数据
- 60% 把握的：D4 / D14 / D15 实际严重性需要 git history 或行为数据

### 跨范围盲区
- AgentTool 定义文件本身（A1 范围边缘 + A2 范围边缘，两人都没专攻）
- OAuth state 归属（属于 ai 包还是 agent 包？）
- compaction 的 `branch-summarization.ts` 内部（A1 跳过）
- session 持久化的 jsonl 实现细节（A1 跳过 storage）

---

## 6. Meta Findings（关于过程本身）

### M1 — Question-First 协议 + §5 self-doubt 联动
两份子报告 question-first 后**各自自我修正了关键论点**：
- A1 §3.2：原"loop mutate 调用方 context.messages" → 取证后发现是 spread copy，自我修正为"loop-internal-mirror-is-invisible-exhaust"（D7）
- A2 Q2：原"v2 禁切 provider 即砍 200 行" → 取证后发现 70% 是同 provider 内必需归一化，自我修正为"砍 ~50 行"
- A2 Q3：ToolDefinition severity 重度 → 轻度

每次修正都是"问题强迫具体证据收集（grep + file:line）"触发的。
**一手报告准确率约 85%；question-first 推到约 98%**。
**对 tau 的影响**：这套方法论本身是 Phase 2 工作流的强候选——任何分析报告都该有 §5 self-doubt + 一轮强制 question-first。

### M2 — pi 缺少架构原则文档
AGENTS.md 只是 contributor 操作手册，没有架构哲学章节。设计意图散在 `packages/agent/docs/` 和代码内。**对 tau 的影响**：tau 必须有一份 first-class 的"design principles"文档（这是 Phase 2 的产出）——不能让 contributor 靠考古来理解设计边界。

### M3 — 作者主动绕过自家规则（D3 升级）
biome.json:13 主动 `"noExplicitAny": "off"` —— 这不是"忘了加 lint"，是**作者主动制造 docs vs 工具链的脱钩**。这告诉 tau：**红线必须从 day-1 就 lint-enforced，且 lint 配置本身要有 review gate**。

---

## 7. Phase 2 入口约束 → 已拆分

本节原内容（10 条 Phase 2 硬约束 C1-C10 + 待验证必抄 + 阻塞与开放项）已拆出独立交接文档：

📄 **[`phase1-to-phase2-handoff.md`](./phase1-to-phase2-handoff.md)**

理由：phase 间交接契约本身是独立交付物；Phase 2 的 coordinator（不应由本 phase coordinator 兼任，防 anchoring bias）应**先读 handoff 再读本主稿**，避免被 Phase 1 视角污染。

---

## 8. Phase 2 是否建议推进？

**建议：推进。** 但有 2 个阻塞需要 parent / 用户确认：

1. **🔴 阻塞 1**：tau 目标语言确认。Criterion A 的整张移植表假设 Go。**用户未亲口确认**。如非 Go，§2 表需重做。
2. **🟡 阻塞 2 (软)**：tau 范围确认。当前 working assumption = agent core + provider 抽象。如用户希望含产品层（coding-agent 类），coding-agent 需要更深抽样（A2 当前仅轻取样）。

不阻塞推进的开放项（可在 Phase 2 中处理）：
- AgentTool 形状跨范围盲区（Phase 2 设计 tool 协议时补读）
- B8 Skills 有效性验证（Phase 2 验证或推迟到产品层）
- B9 schema 机制选型决策（Phase 2 必须做）

---

## 文档版本与修订历史

- v1.0 初版整合稿（基于 A1 v1.1 + A2 v1.1）
- A1 子报告经 question-first 一轮挑战后自我修正 §3.2、行号修正 §3.3、§0 加修订记录
- A2 子报告经 question-first 一轮挑战后自我修正 §3.5（重→轻）、§4.4（200 行→50 行），新增 §3.4 meta-debt 段，§0 加修订记录
- A1 + A2 已 stand down；Phase 2/3/4 如需可再激活

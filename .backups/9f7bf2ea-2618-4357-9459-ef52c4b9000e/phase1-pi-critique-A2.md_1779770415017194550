# Phase 1 / A2 — provider + tool + skills 批判性分析

> 范围：`packages/ai/`（provider 抽象 + 消息归一化 + faux）+ `packages/coding-agent/`（tool 协议 + skills + extension 注册）。
> **不**包含：`packages/agent`（agent loop / pi-agent-core，是 A1 范围）、`packages/tui`（终端 UI，超范围）。
> 所有结论基于 pi 源码二次验证。pi/docs/ 文档**不作为**证据。

---

## 0. 我读了什么、没读什么

**读完的（全文）**：
- `packages/ai/src/index.ts`、`types.ts`（575 行，**provider 抽象核心**）、`stream.ts`、`api-registry.ts`、`session-resources.ts`、`utils/event-stream.ts`、`providers/faux.ts`（499 行，测试基石）、`providers/register-builtins.ts`、`providers/transform-messages.ts`、`providers/simple-options.ts`
- `packages/coding-agent/src/core/skills.ts`（487 行）
- `packages/coding-agent/src/core/tools/index.ts`、`tools/tool-definition-wrapper.ts`
- `pi/AGENTS.md`（作者自述）

**抽样的**（不通读）：
- `providers/anthropic.ts`（1212 行）：head/tail + 关键 grep（claude-code stealth、cache、`as any`、process.env、convertTools/mapStopReason）。**理由**：抽象层已经在 types.ts 定型，具体协议适配的 1212 行不是 critique 重点。
- `providers/openai-completions.ts`：仅 grep（`as any` 分布、compat 字段使用点）。**理由**：同上。
- `core/agent-session.ts`（3089 行）：定向读 tool-hook 安装段（396-440）、tool-registry 字段（300-320、760-810）、`_refreshToolRegistry`（2240-2340）。**理由**：3089 行包含太多产品逻辑（compaction / branch / steering），与 tool 协议无关。
- `core/extensions/types.ts`（1567 行 / 54k 字节）：读 ToolDefinition（426-481）、ExtensionContext.registerTool/registerProvider（1130-1280）；事件 enum 头部段。**理由**：事件具体语义不是 critique 重点。
- `core/extensions/loader.ts`：仅看 `registerTool` 实现（192）。
- `coding-agent/test/suite/harness.ts` 头 100 行 + `packages/ai/test/faux-provider.test.ts` 头 80 行 — 体感测试 setup 体量。

**跳过的**：
- 所有具体 provider 实现（bedrock、google、mistral、openai-codex-responses、azure）—— 抽象形状一旦确定，复制粘贴的协议代码不影响 tau 的设计判断。
- `coding-agent/src/main.ts`（23k 字节）、`config.ts`、`migrations.ts`、`cli/`、`modes/interactive/`、`modes/rpc/`、`modes/print-mode.ts`、所有 UI/footer/keybindings 代码 —— 产品层逻辑，超出范围。
- `extensions/runner.ts`（32k 字节）：只 grep。**理由**：runner 是 lifecycle/事件 dispatch，不是 tool 协议本身。
- `core/compaction/`、`core/export-html/`、`core/bun/`、`package-manager*` —— 范围外。

> **修订记录**：本报告经 question-first 一轮挑战，§3.1 严重度确认、§3.5 严重度从重度降为轻度（理由替换为模块图耦合 + OCP 弱违反）、§4.4 修正"砍 200 行"为"砍 ~50 行"、§3.4 增加 meta-debt 标注（pi 缺自动化 rule enforcement）。

---

## 1. 鸟瞰

### packages/ai 暴露的抽象（顶层 4 个东西）

1. **`Model<TApi>` + `Context` + `StreamOptions`** 三元组（`types.ts:467`、`281-285`、`75-138`）—— "无状态请求材料"。`Model` 描述模型/计费/上下文窗口；`Context = { systemPrompt?, messages, tools? }`；`StreamOptions` 是 transport 旋钮（apiKey/signal/cacheRetention/sessionId/onPayload/onResponse/timeoutMs/...）。
2. **`StreamFunction<TApi, TOptions> = (Model, Context, Options) => AssistantMessageEventStream`**（`types.ts:191-199`）。核心契约是**纯函数**：`Model + Context + Options → 事件流`。
3. **`AssistantMessageEvent` 流协议**（`types.ts:295-306`）—— 11 种事件：start / {text,thinking,toolcall}_{start,delta,end} / done / error。done 携带 `AssistantMessage`（最终结果），error 也携带 `AssistantMessage`（含 `stopReason: "error"|"aborted"` + `errorMessage`）。
4. **`stream(model, context, options)` 顶层入口**（`stream.ts:24-31`）：查全局 `apiProviderRegistry`（按 `model.api` 字符串）→ 调对应 `StreamFunction`。

派生：`Tool<TParameters extends TSchema>`（`types.ts:281-285`）—— **TypeBox schema 直接是 LLM tool 参数**。`Message = UserMessage | AssistantMessage | ToolResultMessage`（`types.ts:266`）—— 跨 provider 统一消息形式。

### packages/coding-agent 的 tool / skills 子系统

**Tool 形状**（`core/extensions/types.ts:426-481`）：`ToolDefinition<TParams, TDetails, TState>` 是**胖接口**，同一份定义包含：
- LLM 协议层：name / description / parameters: TSchema / execute(toolCallId, params, signal, onUpdate, ctx) → AgentToolResult
- 执行控制：executionMode `"sequential" | "parallel"`、prepareArguments shim
- Prompt 元数据：promptSnippet / promptGuidelines（注入 system prompt）
- UI 渲染：renderCall / renderResult（返回 TUI `Component`，需要 `Theme`）
- UI 框架：renderShell `"default" | "self"`、label

**Tool 注册**（`extensions/loader.ts:192`）：extension 调 `pi.registerTool(definition)` → 写到 `extension.tools` Map → `agent-session._refreshToolRegistry()`（`agent-session.ts:2253-2317`）合并：内建工具 + extension 工具 + SDK customTools，输出**两份并行 Map**：`_toolDefinitions`（保留 ToolDefinition 全量，含 prompt/render）+ `_toolRegistry`（裁剪为 `AgentTool` 给 agent core 用）。

**Tool 执行**：agent-session 把 `agent.state.tools = AgentTool[]` 交给 `pi-agent-core`（A1 范围）的 Agent；Agent 触发 `beforeToolCall / afterToolCall` 钩子（`agent-session.ts:397-441`），coding-agent 在钩子里 emit 事件给 extension。**execute 真正调用在 pi-agent-core 内部**，coding-agent 不直接拥有 dispatch 循环。

**Skills**（`core/skills.ts`）：**不是** plugin，**不是** prompt 注入工具调用，**是文件约定**：
1. `~/.pi/skills/` 或 `<cwd>/.pi/skills/` 或 `--skill <path>` → 扫描 SKILL.md（带 YAML frontmatter `name` + `description`）
2. `formatSkillsForPrompt`（`skills.ts:308-340`）把 `<available_skills><skill><name>...<description>...<location>...</skill></available_skills>` 注入 system prompt
3. 当 LLM 觉得任务匹配某 skill 时，**自己调 `read` tool 把 SKILL.md 完整文件读进来**

注册有 **collision detection**（`skills.ts:425-446`）：同名 skill 后注册的不替换，写 collision 诊断；symlink 通过 realpath 去重。

---

## 2. 优点（值得 tau 借鉴）

### 2.1 Stream 函数 = (Model, Context, Options) → EventStream，错误进流不抛

**是什么**（`types.ts:191-199`）：
```ts
export type StreamFunction<TApi, TOptions> = (
  model: Model<TApi>, context: Context, options?: TOptions,
) => AssistantMessageEventStream;
// "Once invoked, request/model/runtime failures should be encoded in the
//  returned stream, not thrown."
```
错误终止用 `{type: "error", reason: "aborted"|"error", error: AssistantMessage}` 事件（`types.ts:305`），最终消息携带 `stopReason` + `errorMessage`。

**为什么真好**：
- **可测试性**：StreamFunction 是普通函数，无 `this`、无构造、无生命周期。`faux.ts:330-380` 整个 faux provider 也只是 `(model, context, options) => stream`。测试只需要 `setResponses([...])` 排好剧本。
- **抽象不漏**：调用方永远是 `for await (const ev of stream)` 或 `await stream.result()`。OpenAI SSE / Anthropic 事件 / WebSocket 这些细节被 11 种事件类型完全屏蔽（`types.ts:296-306`）。
- **错误路径地位**：错误是**值**而不是异常。`{stopReason: "error", errorMessage}` 是 AssistantMessage 的字段（`types.ts:259-260`），与正常消息同形 —— 上层不需要 try/catch 包裹流，状态机用一个 switch 处理 done/error 即可（`utils/event-stream.ts:69-81`）。

**tau 是否借鉴**：**强烈是**。这是 pi 最值得抄的设计。Go 等价：`func(ctx context.Context, model Model, prompt Prompt) <-chan Event` + Event 是 sum type，内含一个表示终态的 `Done` 或 `Err` —— 错误在通道里送，不是 panic 上去。

---

### 2.2 Faux provider 与真 provider 走完全相同的注册器

**是什么**（`faux.ts:339-343` + `api-registry.ts:64-77`）：
```ts
registerApiProvider({ api, stream, streamSimple }, sourceId);
```
faux 用 `randomId(DEFAULT_API)` 生成唯一 api 字符串（`faux.ts:323`），每次注册新 api（避免与全局内建冲突），`unregister()` 用 sourceId 精确清理（`api-registry.ts:84-88`）。

测试用法非常薄（`packages/ai/test/faux-provider.test.ts:30-45`）：
```ts
const reg = registerFauxProvider();
reg.setResponses([fauxAssistantMessage("hello world")]);
const response = await complete(reg.getModel(), context);
```

**为什么真好**：
- **可测试性 #1**：测试不 mock 网络层、不 mock SDK、不 patch import —— 直接通过同一个 `registerApiProvider` API。faux 走的是和真 provider **同一个代码路径**（`stream(model, ...)` → `getApiProvider(api)` → 调 stream 函数）。这意味着真 provider 的注册/dispatch 逻辑也被同时验证。
- **可演化性**：faux 还做 token usage 估算（`faux.ts:179-211`，含基于 sessionId 的伪 prompt cache），所以可以测 cost/cache 计费逻辑。
- 体感证据：harness.ts:94 `registerFauxProvider({models})` —— coding-agent 整个 test/suite 也是这条路径，没有第二种 mock 机制。

**tau 是否借鉴**：**强烈是**。这是 v2 文档 07-testing-faux-provider.md 该坚守的设计。Go 等价：`RegisterProvider(name string, p Provider)` + 测试包提供 `NewFaux()` 实现同一个 `Provider` 接口；测试用 `t.Cleanup(func() { faux.Close() })` 清理。

---

### 2.3 Tool parameters = TypeBox TSchema：一份定义，三处用

**是什么**（`types.ts:281-285`）：
```ts
export interface Tool<TParameters extends TSchema = TSchema> {
  name: string; description: string; parameters: TParameters;
}
```
`tools/read.ts:21-26`:
```ts
const readSchema = Type.Object({
  path: Type.String({ description: "..." }),
  offset: Type.Optional(Type.Number(...)),
  limit: Type.Optional(Type.Number(...)),
});
export type ReadToolInput = Static<typeof readSchema>;
```
- Schema 本身是 JSON Schema 兼容对象（直接发给 LLM）
- `Static<typeof schema>` 编译期推出 TS 类型 `ReadToolInput`
- `execute(params: Static<TParams>, ...)`（`extensions/types.ts:455-461`）—— execute 拿到的就是这个 TS 类型

**为什么真好**：
- **抽象不漏**：作者不手写 JSON Schema，也不手写 TS 类型，更不需要"schema 与类型同步"这种永恒债。
- **可演化性**：加新参数改一处 `Type.Object`，TS、JSON Schema、execute 签名同步更新。
- 反例：openai-completions.ts:996 `parameters: tool.parameters as any  // TypeBox already generates JSON Schema` —— 这处 `as any` 注释证明：在 provider 适配点，TypeBox 输出已经是有效 JSON Schema，**不需要转换**。

**tau 是否借鉴**：**是，但形式要换**（Go 没有 TypeBox 等价物）。原则借鉴：**禁止"schema 一份 + Go 结构体一份"双写**。可选路径：(a) 用 Go reflect + struct tag 自动生成 JSON Schema（jsonschema 库）；(b) 反向 —— 用 schema 文本作为 single source of truth，go generate 出 struct。pi 选择 schema-first，理由是 LLM 看 schema、人看类型，schema 是契约。

---

### 2.4 Skills = SKILL.md 文件 + LLM 自主 read，不是 plugin

**是什么**（`skills.ts:308-340`）：把每个 skill 的 name/description/location 用 XML 注入 system prompt，**仅此而已**。SKILL.md body 不进 prompt。LLM 看到描述觉得任务匹配，**自己调 read tool** 把 SKILL.md 完整文件加载进上下文。

完整调用链：discovery（`loadSkills`）→ frontmatter 校验（`validateName`、`validateDescription`，`MAX_NAME_LENGTH = 64`、`MAX_DESCRIPTION_LENGTH = 1024`，符合 Anthropic Agent Skills spec）→ collision detection（`skills.ts:425-446`，realpath 去重 + 同名诊断）→ 注入 prompt。

**为什么真好**：
- **抽象不漏**：用户不学新 plugin API，写 markdown 即可。前端无需理解什么是 skill 调用。
- **可演化性**：加 skill = 加文件，零代码改动。
- **隐式状态/全局**：skills 是从磁盘扫的纯数据，没有运行时状态；`disable-model-invocation` 字段允许只暴露给 `/skill:name` 显式触发，不进 prompt（`skills.ts:312-314`）。
- **错误路径**：解析失败 → `ResourceDiagnostic[]`（`skills.ts:79-82`），不抛错；missing description → 跳过该 skill 但其他继续。
- **极简的优雅**：用现有的 read tool 完成"按需加载"，没有发明新机制。

**tau 是否借鉴**：**强烈是**。前提是 tau 已经有一个像样的 read tool。文件约定优于代码扩展点，是"对扩展开放、对修改关闭"的好实例。

---

### 2.5 transform-messages：跨 provider 消息归一化是真复杂度

**是什么**（`transform-messages.ts:60-219`）：把 internal `Message[]` 转成 provider 可接受形式时处理：
1. **不支持图像的模型**（`downgradeUnsupportedImages`，34-58）：图像替换为占位文本，相邻图像合并为一个占位（避免噪声）。
2. **跨模型 thinking blocks**（85-105）：同一模型保留（含 thinkingSignature 用于回放）；redacted thinking 跨模型丢弃（opaque encrypted，会被对方 API 拒）；非 redacted 跨模型转换为普通 text（保留语义，丢失签名）。
3. **跨模型 tool_call_id**（114-124）：OpenAI Responses 生成 450+ 字符带 `|` 的 ID；Anthropic 要求 `^[a-zA-Z0-9_-]+$ ≤64 字符`。`normalizeToolCallId` 重命名后建立 `toolCallIdMap`，`toolResult.toolCallId` 同步重写。
4. **孤儿 toolCall 注入合成 result**（145-184）：assistant 有 `toolCall` 但下一条不是匹配 `toolResult`（用户中断 / 错误恢复），插入合成 `toolResult{ content: "No result provided", isError: true }`，否则 OpenAI 会报 "reasoning without following item"。
5. **跳过 errored/aborted 的 assistant message**（170-176）：不重放部分 reasoning，让模型从最后有效状态重试。

**为什么真好**：
- **可演化性**：所有的"跨 provider 接续"逻辑被压缩到一个文件、一个函数。每个 provider 在 stream 入口先调 `transformMessages` 再适配。
- **错误路径**：上述每条都是真实碰到过的失败模式；注释里清晰说明 "May have partial content...Replaying them can cause API errors"，这是产品级经验沉淀。

**tau 是否借鉴**：**借鉴形式，谨慎借鉴具体规则**。tau 应该认识到：跨 provider 接续 ≠ 简单格式转换。要么明确**禁止跨 provider handoff**（简化巨多），要么从 day 1 就规划归一化层。**别让归一化逻辑散布在每个 provider 适配器内**。

---

### 2.6 AssistantMessageEventStream：有限状态机比 callback hell 好

**是什么**（`utils/event-stream.ts:4-65`）：通用 `EventStream<T, R>`，构造时指定 `isComplete` 和 `extractResult`，自动维护：(1) `for await` 异步迭代，(2) `result()` Promise（一次终态自动 resolve），(3) `push()` 在 done/error 后自动忽略后续事件。

**为什么真好**：
- **可测试性**：消费者代码通常长这样：
  ```ts
  for await (const ev of stream) { /* update UI */ }
  const final = await stream.result();
  ```
  既可以"看每个 delta"又可以"等最终 message"，**同一个流对象**支持两种消费模式。
- **错误路径地位**：error 也是流里的事件，"流结束" + "拿到最终 message" 不论成功失败都同形（`event-stream.ts:69-81`）。
- **并发**：基于 Promise + 队列，无 `EventEmitter` 隐式订阅风险（订阅顺序、漏取早期事件）。

**tau 是否借鉴**：**借鉴模式**。Go 等价：buffered channel + 一个 `Result()` 方法返回 `<-chan Event` 同时另一个 `Wait() (Final, error)`。坑：Go 里如果做"既能 range 又能 Wait"，要小心 `Wait` 不能消费 channel 否则 range 看不到事件 —— pi 用队列 + 多 waiter 解决，Go 等价是一次 fanout。

---

**所有优点的硬准则归属**：
| # | 决策 | 测试性 | 演化性 | 抽象不漏 | 隐式状态 | 错误路径 | 并发 |
|---|---|---|---|---|---|---|---|
| 2.1 | StreamFunction 纯函数 + 错误进流 | ✓ | ✓ | ✓ |  | ✓✓ |  |
| 2.2 | faux 走真注册路径 | ✓✓ |  | ✓ |  |  |  |
| 2.3 | TypeBox schema 单源 |  | ✓ | ✓ |  |  |  |
| 2.4 | Skills = 文件 + LLM 自主 read |  | ✓✓ | ✓ | ✓ | ✓ |  |
| 2.5 | transform-messages 集中归一化 |  | ✓ | ✓ |  | ✓ |  |
| 2.6 | EventStream 状态机 | ✓ |  | ✓ |  | ✓ |  |

---

## 3. 缺点 / 架构债（tau 必须避免）

### 3.1 全局 `apiProviderRegistry` 是模块级 Map —— 隐式单例、隐式无状态约定

**是什么**（`api-registry.ts:38`）：
```ts
const apiProviderRegistry = new Map<string, RegisteredApiProvider>();
```
模块级 Map。`registerBuiltInApiProviders()` 在 `register-builtins.ts:406` **模块顶部**（顶层 side-effect）就调用，所以 import 这个文件即注册全部内建。

**为什么是债**：
- **隐式状态/全局** 硬准则直接打到。同一个 Node 进程里，所有 session、所有 AgentSession 实例、所有测试共用一份 registry。
- **多 session/会话隔离**：`stream(model, ctx)` 通过 `model.api` 字符串查 registry → 调 stream 函数。**只有在 stream 函数本身完全无状态时才安全**。这是不写在类型里的隐含约定。一旦某 provider 闭包了状态（OAuth token cache、HTTP keepalive agent），跨 session 就会污染。anthropic.ts:47 `process.env.PI_CACHE_RETENTION` 已经是个例子 —— 它把进程级环境变量做"默认值"绑进了 stream。
- **测试性**：faux 用 `randomId(DEFAULT_API)` 当 api 名规避冲突（`faux.ts:323`）；**说明作者自己也意识到全局污染问题**，但解法是"每次注册新名字"而不是"实例化 registry"。
- **演化性**：要做 per-session provider override，必须新加 SDK 入口（`agent-session.ts:2270` 的 `_customTools` + 单独的 `registerProvider` 通道，绕过全局）—— 复杂度直接转移到 agent-session 这个 god class。

**runtime mutable 的硬证据**（Q1 追问取证）：
- `api-registry.ts:66-78` 暴露 `registerApiProvider(provider, sourceId?)`、`unregisterApiProviders(sourceId)`、`clearApiProviders()` —— **任意时刻可调**，不是 read-once。
- `coding-agent/src/core/model-registry.ts:796-810`：`ModelRegistry.registerProvider(name, config)` 是 extension API（来自 `ExtensionContext.registerProvider`），**runtime 触发**，最终落到全局 `registerApiProvider`（model-registry.ts:873）。同文件 doc 注释：*"Register a provider dynamically (from extensions)"* —— **作者明知 mutable**，但没提供 scoped registry。
- 同进程多 AgentSession 场景：session A 加载的 extension 注册 provider X → session B 立即可见 X。**这是 multi-session-safety 的核心反例典型**：纸面假设无状态，运行时是 mutable 共享。

**tau 怎么避免**：
- **registry 实例化**。`type ProviderRegistry interface { Register(api string, p Provider); Resolve(api string) Provider }`，每个 Session 持有一个 registry，或顶层有 default registry 但**Session 可注入自己的**。
- 如果坚持全局便利，至少把"无状态"约束写进类型（在 Go 里就是不让 `Provider` 有可变字段，stream 方法只接 ctx 不存 ctx）。
- **永远禁止 provider 内部读 process.env**。env 应该在配置加载层映射成 `Options`，传进 stream。

---

### 3.2 OpenAICompletionsCompat：18 字段开关字典 = "兼容地狱被字典化"

**是什么**（`types.ts:319-372`）：18 个字段（不全列）：
```ts
supportsStore? supportsDeveloperRole? supportsReasoningEffort?
supportsUsageInStreaming? maxTokensField? requiresToolResultName?
requiresAssistantAfterToolResult? requiresThinkingAsText?
requiresReasoningContentOnAssistantMessages?
thinkingFormat?: "openai"|"openrouter"|"deepseek"|"together"|"zai"|"qwen"|"qwen-chat-template"
openRouterRouting? vercelGatewayRouting? zaiToolStream?
supportsStrictMode? cacheControlFormat?: "anthropic"
sendSessionAffinityHeaders? supportsLongCacheRetention?
```
然后在 openai-completions.ts 用 `(params as any).enable_thinking = ...`、`(params as any).chat_template_kwargs = ...` 等 20+ 处 `as any` 把这些开关展开成具体的请求字段。

**为什么是债**：
- **抽象漏**：兼容方的 quirks（OpenRouter 路由、DeepSeek thinking 字段、Together 的 `reasoning: { enabled }`、qwen 的 chat_template_kwargs、zai 的 tool_stream）泄漏到了**共享的 types.ts**。一个新兼容方进来 → 加一个枚举值 → 一个 `if` 分支 → 一处 `as any`。
- **演化性差**：每个兼容方独立的 schema 应该是独立的 provider，但被强行合并在 "openai-completions" 一个 api 名下。`types.ts:332` 的 `thinkingFormat: "openai"|"openrouter"|"deepseek"|"together"|"zai"|"qwen"|"qwen-chat-template"` —— **每加一个兼容方就改一次共享枚举**。
- **类型撒谎**：openai-completions.ts 全文 20+ `(params as any).<field> = ...`（line 526/535/548/564/566/568/573/575/599/603/609/619/857/890/936/996）。意思是：静态类型说"这是 OpenAI completions params"，但运行时可以塞任何字段。

**tau 怎么避免**：
- **每个 provider 一个独立的 stream 函数 + 独立的 Options**，不要做"超集兼容层"。如果 OpenRouter / DeepSeek 行为差异大，它们就是不同的 provider。
- 真有共性 → 共享**helper 函数**（"build chat-completion payload"），但每个 provider 自己组装最终 payload。**禁止用一个共享 type 配 18 个 boolean 来覆盖差异**。
- 如果硬要统一接口，那 `Options` 里允许 `extra: map[string]any` 兜底，但这种逃生口必须显式标记，不能让类型表面"看起来管"实际不管。

---

### 3.3 类型撒谎：`as any` 在 provider 边界系统性出现

**是什么**（grep `/packages/ai/src/providers/`）：
- openai-completions.ts: 20+ 处 `(params as any).XXX = ...`
- anthropic.ts:629/1146/1155
- google.ts:371/415/418/421（`thinkingLevel: "LOW" as any`）
- mistral.ts:611（`return choice as any`）
- openai-codex-responses.ts:761（`super(url, {...} as any)`）
- openai-responses-shared.ts:274 + openai-completions.ts:996：`tool.parameters as any  // TypeBox already generates JSON Schema`

**为什么是债**：
- **类型撒谎** 红旗直接命中。静态类型说"这是 SDK 的 MessageCreateParams"，运行时可以塞 `enable_thinking`、`thinking`、`tool_stream`、`provider`、`providerOptions` 等 SDK 不知道的字段。
- 注释 `// TypeBox already generates JSON Schema` 是合理的（TypeBox 输出和 JSON Schema 兼容），但**用 `any` 绕过类型检查**而不是建一个 `ToJSONSchema` 类型函数，是工程妥协。
- 风险：SDK 升级新字段类型变化 → `as any` 不报错 → bug 进生产才被发现。

**tau 怎么避免**：
- **禁止 `interface{}` / `any` 在 provider 边界出现**（除非有明确注释说明哪个 SDK 字段、为什么转换）。
- 用 `map[string]any` 显式承认 "这里我不打类型"，比 `cast.any` 诚实。
- TypeBox-like 库在 Go 里：考虑 `jsonschema` 包 + 反射，或定义独立的 `Schema` 类型（不直接复用 SDK 类型）。
- 设立 lint：禁止 `as any`、`interface{}` 在指定路径出现。

---

### 3.4 register-builtins 的 dynamic import 违反 pi 自家规则

**是什么**（`register-builtins.ts:198-280`）：
```ts
function loadAnthropicProviderModule() {
  anthropicProviderModulePromise ||= import("./anthropic.ts").then((module) => ({...}));
  return anthropicProviderModulePromise;
}
// ... 同样模式 9 处（azure, google, google-vertex, mistral,
//                     openai-codex-responses, openai-completions,
//                     openai-responses, bedrock）
```
每个内建 provider 都是 `import("./xxx.ts")` 懒加载；`createLazyStream` 包成"调用时再加载"的 stream 函数，把内部 stream 用 `forwardStream` 桥接给外层 `AssistantMessageEventStream`（`register-builtins.ts:128-145`）。

`pi/AGENTS.md:14`:
> **No inline imports** (`await import()`, `import("pkg").Type`, dynamic type imports). Top-level imports only.

规则**没有**写豁免清单。

**为什么是债（或至少是不一致）**：
- **自家规则的明显违例**。要么这是历史遗留（规则后定），要么作者豁免了 lazy provider 加载这个特殊用例（未文档化）。
- 设计上的合理性：bedrock 用 AWS SDK（重）、google 用 GenAI SDK 等。冷启动不加载所有 SDK 是真收益。但**这种合理性应该写在豁免说明里**，而不是默认违规。
- 风险：tree-shaking 失效（捆绑器不知道动态 import 链）；冷启动时 import 失败被吞进 stream 错误事件（`createLazyLoadErrorMessage`，146-159）—— 用户看到的不是 "Module not found"，而是 "Request failed: ..."，定位变难。

**tau 怎么避免**：
- 规则要么**完全禁止** dynamic import，要么**明确列出**豁免点（"provider 模块允许；其他位置禁止"）。
- 如果懒加载是真需求 → 用 build-time code splitting / Go 的 build tag —— 把"懒"做在打包/构建层，不做在运行时。
- Go 里这个问题不存在（无 dynamic import），但等价问题是 init() side effect 不可控；解法是显式 `RegisterBuiltins(reg)` 调用，而不是 `import _ "..../bedrock"`。

**🚩 META-DEBT — pi 缺自动化的 rule enforcement**：

这条违例本身揭示一个**元层面问题**：pi 的设计纪律有维护缺口。红线写在 AGENTS.md，**代码里没 lint rule 兜底**：
- `biome.json:13` 显式 **`"noExplicitAny": "off"`** —— AGENTS.md 第 9 行说 "No `any` unless absolutely necessary"，但作者**主动关掉**了 lint 检查。
- 全仓 `as any` 在 `packages/ai/src/` + `packages/coding-agent/src/` 共 **41 处**（已排除 generated）—— 这是关闭检查后的自然结果。
- AGENTS.md 第 14 条 "No inline imports" 同样无 lint 规则保护，9 处违例（§3.4 主体）一直存在。

**对 tau 的启示**：纸面规则 + 关掉的 lint = 规则形同虚设。tau 必须做到：每条红线（无 `any`、无 inline import、无 `os.Getenv` 在 provider 层、provider 形状一致性）都对应**一条自动化检查**（golangci-lint 自定义规则、go vet、CI grep gate），否则规则会随时间退化。

---

### 3.5 ToolDefinition 接口杂（**轻度债** — 模块图耦合 + OCP 弱违反）

**是什么**（`extensions/types.ts:426-481`）：
```ts
export interface ToolDefinition<TParams, TDetails, TState> {
  // LLM 协议
  name; description; parameters: TParams;
  execute(toolCallId, params, signal, onUpdate, ctx): Promise<AgentToolResult>;
  // 执行控制
  prepareArguments?; executionMode?;
  // Prompt 元数据
  promptSnippet?; promptGuidelines?;
  // UI 渲染（Optional 但 type 已 import）
  label; renderShell?; renderCall?; renderResult?;
}
```
而该文件的 import（`extensions/types.ts:31-43`）：
```ts
import type { Component, EditorComponent, EditorTheme, KeyId, ... } from "@earendil-works/pi-tui";
import type { Theme } from "../../modes/interactive/theme/theme.ts";
```

**为什么是债**：
- **接口臃肿** 红旗命中。一个 interface 同时定义了：(a) LLM 可调用的契约、(b) UI 渲染、(c) prompt 注入元数据、(d) 执行调度策略。**违反单一职责**。
- **抽象漏**：renderCall/renderResult 让 ToolDefinition 模块依赖 `pi-tui` 和 `modes/interactive/theme/`。即使 `print-mode` 或 `rpc-mode` 完全不渲染 TUI，类型导入还是把这些拉进了模块图。
- **演化性**：写一个无 UI 的服务端 tool，仍要从 ToolDefinition 接口里 "省略" UI 字段 —— 它们是 Optional 缓解了一点，但 ToolDefinition 这个名字现在"不只是 tool 定义"。
- **可测试性**：harness.ts 里 `tools: AgentTool[]` 用的是 `pi-agent-core` 的 AgentTool（精简版，无 render），所以测试里 ToolDefinition 完整形不被覆盖 —— 进一步说明 render 字段不属于 LLM 协议层。

**tau 怎么避免**：
- **三个独立类型**：
  1. `ToolSpec`（LLM 协议层）：name + description + schema + execute。**不依赖任何 UI 包**。
  2. `ToolPromptMeta`：promptSnippet / promptGuidelines。可选挂在 ToolSpec 上。
  3. `ToolRenderer`（UI 适配，单独包）：注册"how to render tool X"，与 ToolSpec 解耦。
- tool 注册器持有 ToolSpec；renderer 注册器持有 ToolRenderer。两者独立演化。
- Go 里特别自然：`type Tool interface { Name() string; Schema() Schema; Execute(...) Result }`，render 是另一个 package 的 visitor。

---

### 3.6 AgentTool ↔ ToolDefinition 双类型，必须 wrapper 桥接

**是什么**（`tools/tool-definition-wrapper.ts:5-42`）：
- `pi-agent-core` 暴露 `AgentTool` 接口（A1 范围）—— 精简版，给 agent loop 用。
- `coding-agent` 自定义 `ToolDefinition` —— 胖版，含 render/prompt。
- 双向适配：`wrapToolDefinition`（ToolDefinition → AgentTool，丢 render/prompt）+ `createToolDefinitionFromAgentTool`（AgentTool → 最简 ToolDefinition，**含 `as any`**：`parameters: tool.parameters as any`）。

**为什么是债**：
- **分层债**。两个 package 各定义自己的 tool 形状，靠 adapter 互转。新增字段 → 改两边 + 改 adapter + 改 `as any`。
- 体现在 agent-session.ts:300-320 同时维护 `_toolRegistry: Map<string, AgentTool>` 和 `_toolDefinitions: Map<string, ToolDefinitionEntry>` 两份并行 Map，`_refreshToolRegistry` (2253-2317) 必须同步重建两份。**任何 tool 改动改两处**。
- **可演化性**：极差。加一个 "tool 必须有 X 字段" 需求：改 ToolDefinition、改 AgentTool、改 wrapper、改 _refreshToolRegistry、改 unwrap 路径。

**tau 怎么避免**：
- **agent core 和应用层共用同一个 `Tool` 类型**，应用层通过 `embed` 或 metadata sidecar 加扩展信息（promptSnippet / renderer 注册到独立的 map），不要建并行类型。
- 如果 agent core 必须通用（要支持非 coding 场景）→ agent core 只持有 `Tool { name, schema, exec }`，应用层维护 `metadata: map[ToolName]Metadata`，**不重新定义 Tool**。
- 单一权威 registry。不要为同一份 tool 维护两份 Map。

---

### 3.7 createTool / createCodingTools / ... — 7 个手写组合工厂

**是什么**（`tools/index.ts:81-196`）：
- `createTool(name, cwd, opts)` —— switch 派发（`tools/index.ts:113-135`）
- `createToolDefinition(name, cwd, opts)` —— 同样的 switch 派发（96-115）
- `createCodingTools` / `createReadOnlyTools` / `createAllTools` —— 3 个手写组合（共 7 工具）
- `createCodingToolDefinitions` / `createReadOnlyToolDefinitions` / `createAllToolDefinitions` —— 镜像 3 个

**为什么是债**：
- **可演化性差**：加新 tool（比如 `WebFetch`）改动半径 = 7 处：
  1. `createReadToolDefinition` 旁加 `createWebFetchToolDefinition`
  2. `ToolName` 联合类型加 `"webfetch"`
  3. `allToolNames` Set 加成员
  4. `ToolsOptions` interface 加 `webfetch?`
  5. `createToolDefinition` switch 加 case
  6. `createTool` switch 加 case
  7. 决定进哪些组合包：`createCodingTools` / `createReadOnlyTools` / `createAllTools`（最多 6 个分支）
- **DRY 失败**：`createAllTools` 和 `createAllToolDefinitions` 字段一一镜像。
- **Tool / ToolDefinition 双轨**直接放大此处的代价（每加一个工具都改两份并行结构）。

**tau 怎么避免**：
- **注册中心模式**：tool 包自己注册到一个 `ToolRegistry`，组合包通过 query（"所有读类工具"、"所有编辑类工具"）派生。
  ```go
  type ToolRegistry interface {
    Register(t Tool)
    ByCategory(c Category) []Tool
  }
  ```
- 工具加 `Category` 标签（`read | mutate | search | shell`），读取/写入子集是过滤而非手写枚举。
- 加新工具：1 个 `RegisterFooTool(reg)` 调用，0 改动其他位置。

---

### 3.8 anthropic.ts 的 stealth mode：硬编码 Claude Code 工具名

**是什么**（`anthropic.ts:69-89`）：
```ts
const claudeCodeVersion = "2.1.75";
const claudeCodeTools = [
  "Read", "Write", "Edit", "Bash", "Grep", "Glob",
  "AskUserQuestion", "EnterPlanMode", "ExitPlanMode",
  "KillShell", "NotebookEdit", "Skill", "Task", "TaskOutput",
  "TodoWrite", "WebFetch", "WebSearch",
];
const ccToolLookup = new Map(claudeCodeTools.map((t) => [t.toLowerCase(), t]));
const toClaudeCodeName = (name: string) => ccToolLookup.get(name.toLowerCase()) ?? name;
```
然后在 `convertTools`（tail 段）：`name: isOAuthToken ? toClaudeCodeName(tool.name) : tool.name` —— OAuth 模式下强制把 tool 改名为 Claude Code 的工具名。

**为什么是债**：
- **抽象漏 + 隐式状态**：provider 抽象层硬编码了 Claude Code 的工具命名约定。一个 provider 文件知道产品层 17 个工具的具体名字。
- **演化性**：Claude Code 升级、加新 tool、改名 → 要改 anthropic.ts 这个 provider 文件。**provider 不应该知道产品的 tool 名字**。
- 这是产品决策（OAuth 模式下伪装成 Claude Code 以利用其优惠/特性）侵入抽象层的典型案例。可能是真实的商业必需，但**作为 tau 的设计参考时**，这种"hack 写在抽象层"应被避免。

**tau 怎么避免**：
- 如果有此类需求 → **provider Options 接口加一个 `toolNameTransform: (name string) => string` 钩子**，让产品层注入映射。provider 不知道 Claude Code 是什么。
- 或更好：tau 一开始就**规定** tool 命名规范（snake_case 或 PascalCase 二选一），不允许 provider-specific naming。

---

### 3.9 process.env 直读 in provider

**是什么**（`anthropic.ts:47`）：
```ts
function resolveCacheRetention(cacheRetention?: CacheRetention): CacheRetention {
  if (cacheRetention) return cacheRetention;
  if (typeof process !== "undefined" && process.env.PI_CACHE_RETENTION === "long") return "long";
  return "short";
}
```

**为什么是债**：
- **隐式状态/全局** 红旗命中。Provider 内部读进程环境变量 → 不同 process / 不同测试用例之间状态泄漏；测试要 mock 必须 mock process.env，污染整个进程。
- **可测试性**：覆盖这个分支必须 `vi.stubEnv("PI_CACHE_RETENTION", "long")` 之类，比传 option 重得多。
- 这只是抽样发现的一处；其他 provider 可能也有（grep 范围有限，未穷举）。

**tau 怎么避免**：
- **Provider 内部禁止读 env / 全局配置**。env 在配置加载层（cli/config 解析）映射成 `Options.CacheRetention` 等强类型字段，然后传进去。
- Go 等价：禁止 provider 包里出现 `os.Getenv`。

---

### 3.10 transform-messages 注入 "No result provided" 合成 toolResult

**是什么**（`transform-messages.ts:142-184`）：
```ts
result.push({
  role: "toolResult",
  toolCallId: tc.id,
  toolName: tc.name,
  content: [{ type: "text", text: "No result provided" }],
  isError: true,
  timestamp: Date.now(),
} as ToolResultMessage);
```
当 assistant 消息含 toolCall 但下一条不是匹配的 toolResult（用户中断、错误恢复），合成 isError=true 的 result。

**为什么是债（小，但要记录）**：
- **错误路径地位混淆**：合成的 toolResult 是 internal 修复，但**LLM 收到的就是真的 isError + "No result provided"**。模型行为变得依赖于"看到这个特殊字符串如何反应"。如果 LLM 把这个解读成"任务失败需要重试"，会产生意料外行为。
- 注释（172-178）讲了为什么不"重放 errored assistant"，但没解释**为什么注入合成 result 比让 API 报错更好**。
- **隐藏 vs 显式**：替代方案是在 transform 层抛 `IncompleteContextError`，让上层决定（截断到上一个完整 turn / 让用户重启 / 注入合成 result）。pi 选了静默修复，是产品决策但牺牲了可观测性。

**tau 怎么避免**：
- **明确策略**：要么"合成 result 但内容必须是 LLM 不会误解的提示词"（如 `"<tool_call_aborted_by_user_or_runtime>"`），要么"丢弃整个不完整 turn 让上层重新组织"。两者都比当前更明确。
- 加日志/事件：每次注入合成 result 触发 `synthetic_tool_result_inserted` 事件，便于排查"为什么 LLM 突然觉得任务失败了"。

---

## 4. 灰色地带（双面性）

### 4.1 全局 registry vs 实例化 registry

| 维度 | 全局（pi 选择） | 实例化（tau 备选） |
|---|---|---|
| 优 | API 简单：`stream(model, ctx)` 一行 | 隔离干净，多 session 安全 |
| 缺 | 隐含"无状态"约定；testing 要 unregister；多 session 共享一份 | 样板多：每处都要传 registry；DI 复杂度上升 |
| Trade-off | 选简单 + 文档化"无状态约束" | 选纯净 + 接受样板 |

**tau 怎么决策**：**实例化 registry**。tau v2 的 02-harness.md 已经把"显式依赖注入"作为目标。Go 的 typical pattern 是 `New(opts) *Foo` 返回实例，全局只在 `main` 收口。代价是每个 session 持一份 `ProviderRegistry`，但收益是 (a) 测试不需要 unregister，(b) 多租户场景天然安全，(c) 类型不再撒谎"全局是无状态的"。

---

### 4.2 StreamFunction 是函数 vs 类（带方法的 Provider 接口）

**优面（pi 选函数）**：极易组合，可以柯里化、可以 wrap、可以 mock。Faux 一行就成。无构造、无 lifecycle 管理。

**缺面（pi 没选类）**：
- OAuth token 刷新这种状态只能塞 closure（外面 ref 一份）或塞全局（pi 选了 `utils/oauth/` 模块级状态）。
- `streamSimple` 和 `stream` 在 `ApiProvider` interface 里同时声明，但函数式注册让它们无法共享 closure 状态 —— pi 通过 `ApiProvider { api, stream, streamSimple }` 把两个函数捆在一起注册（`api-registry.ts:22-26`），但两者本身是独立函数。

**Trade-off**：函数式简单但状态管理零样板；接口式重但生命周期清晰（Init / Close）。

**tau 怎么决策**：**用接口**（Go 风格自然）。`type Provider interface { Stream(ctx, model, prompt, opts) <-chan Event; Close() error }`。Init 在 `NewXxxProvider(cfg)` 完成，Close 显式回收（OAuth、HTTP 连接池）。如果想要 pi 那种"函数登记"风格，包一层 `FuncProvider(fn)` 适配器。

---

### 4.3 Skills 用 LLM 自主 read vs 强制注入

**优面（pi 选自主 read）**：context 占用最小（系统 prompt 只放 name+description+location）；skill 文件改了不需要重启；skill 多了不会爆 token。

**缺面**：依赖 LLM"懂得 read"；某些便宜模型可能不会主动 read 而是直接编答案；context 缺失诊断难（看不到 LLM 是否读了 skill）。

**Trade-off**：信息密度 vs 行为可靠。

**tau 怎么决策**：**默认借鉴 pi 自主 read**（skill 一般不是关键路径，重要的会高频被读）。但保留**强制注入选项**：skill frontmatter 加 `inject: "auto" | "always"`（always = 直接把 SKILL.md body 进 prompt）。让用户挑哪些 skill 必须可见。

---

### 4.4 transform-messages 跨 provider 复杂度：是真复杂度还是过度设计

**优面**：跨 provider handoff（Anthropic → OpenAI 中间换 model）真的会触发 thinking signature / tool_call_id / image support 的差异，pi 在一处文件里解决。

**缺面**：tau 如果**根本不支持** session 中切 provider，整个 transform-messages 都不需要。

**Trade-off**：v2 想做 multi-provider session 切换 → 必须接受这层复杂度。v2 单 provider session（切 provider 只在新 session）→ 整个 transform 简化为 0。

**tau 怎么决策**：**v2.0 默认禁止 session 中切 provider**。这一刀砍掉，consume 的成本巨大下降。等到真有用户要"会话中切到便宜模型省钱"再加，那时候至少需求清晰、复杂度有 ROI 评估。

---

### 4.5 Compat 字段化 vs 独立 provider per 兼容方

**优面（字段化）**：复用 90% 的 OpenAI completions 适配代码，只用一个 type 表达 quirks。

**缺面（字段化）**：types.ts 共享枚举随兼容方膨胀；`as any` 满天飞；新加兼容方修改共享代码（破坏 OCP）。

**Trade-off**：代码量 vs 抽象清晰度。

**tau 怎么决策**：**走中间路** —— 提供 `BaseChatCompletionProvider` 作为**helper 库**（不是 type），每个兼容方自己 import 这个 helper 拼装请求。共享是函数级的（`buildOpenAIPayload(...)`），不是 type 级的。新加 provider 写一个新文件，不改共享 type。

---

## 5. 我没把握 / 我假设了什么

### 60% 把握的结论
- **3.1（全局 registry）的实际危害**：我推测它会导致多 session 污染，但没找到 bug 报告或具体污染案例。可能 pi 在实际部署中从未遇到，因为 stream 函数实际上做到了无状态（即使没有类型保证）。**风险评估可能偏严**。
- **3.4（dynamic import 违规）的严重性**：我认为这是不一致，但 pi 项目可能内部约定 `register-builtins.ts` 是基础设施豁免点（git history 可能有 issue 讨论）。我没看 git log。**结论可能过严**。
- **3.10（合成 toolResult）的实际行为影响**：我推测 LLM 看到 isError 会触发重试或道歉，但没运行真实模型验证。可能在实际中影响很小（模型对这个特定字符串已经训练过容忍）。

### 没验证的假设
- **`pi-agent-core` 的 `AgentTool` 是什么形状**：我从 coding-agent 引用推断它是精简版（无 render），但**没读 packages/agent 源码**（A1 范围）。3.6 论点假设 AgentTool ≠ ToolDefinition，需要 A1 确认。
- **register-builtins lazy import 是否真的避免了 SDK 加载**：我假设 dynamic import 能让 v8 不解析未访问的模块，但实际效果取决于打包工具配置。tau 用 Go 的话该问题不存在。
- **OpenAICompletionsCompat 的 18 字段是否每个都被用到**：我 grep 看到大部分有引用，但没穷举每个字段的"defaults from URL"逻辑（那是更深的债）。
- **transform-messages 的 `existingToolResultIds` 重置逻辑（line 156）的正确性**：每个 assistant 都重置一次，看起来对，但跨 turn 多 toolCall 的边界情况没逐个走读。

### 没读到位的地方
- **agent-session.ts 3089 行整体结构**。它是 god class（compaction、forking、steering、queue management、tool registry、event dispatch、settings、auth、resource loader 全在一起）—— **god class 本身就是债**，但在我"tool 协议"范围里只摸到一角，没能给出完整批判。**tau 应该单独分析"agent session 是什么形状"**。
- **extensions/runner.ts 32k**。lifecycle event dispatch 的实现细节、handler 异常如何传播、register-by-extension 的隔离性 —— 都没看。tau 设计 extension 系统时应该单独再读。
- **完整 OAuth 流程**。`utils/oauth/` 目录我跳过了。OAuth token cache 的状态归属（全局 vs session）是 3.1 论点的实证，但我没验证。
- **真实 provider 的错误恢复**：anthropic.ts 1212 行里关于 retry / rate-limit / SSE 中断的具体处理我没读。这些是 "错误路径地位" 准则的实证，但只采样了。

### 我预期 parent 会挑战的点
1. "skills 真的'好'吗？没数据证明 LLM 会主动 read SKILL.md" —— 同意，2.4 是基于设计原理的判断，没行为数据。
2. "ToolDefinition 臃肿是真问题吗？renderCall 是 Optional" —— Optional 缓解了运行时影响，但 type import 已经把 TUI 拉进模块图（types.ts:31-43），3.5 主要是"模块图层"的债不是"运行时"的债。
3. "你说 tau 应该 v2.0 禁止 session 中切 provider，会不会太激进" —— 4.4 是建议；如果 v2 已经规划 session 切 provider，把建议降级为"接受 transform-messages 那 200 行复杂度作为入场费"。
